# TNDrop v1.7 テキスト編集機能 実装計画

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** テキスト/リンクカードに編集アイコンを追加し、専用ダイアログで本文を編集して、本文・種別 (UrlDetector 再判定)・重複判定ハッシュを 1 回の解決で同時更新する。

**Architecture:** Core は `ItemStore.UpdateText` 1 メソッドの追加のみ (3 項目同時更新の唯一の入口)。UI は新規 `EditDialog` (PasswordDialog と同じローカル `Dlg.*` リソース流儀) + App の単一インスタンス管理 (BackupDialog と同型) + Cards.xaml のアクションボタン 1 個 + ShelfWindow の class handler に case 1 つ。

**Tech Stack:** .NET 10 / WPF、既存 BCL のみ (追加 NuGet なし)、xunit。

**正とする設計書:** `docs/superpowers/specs/2026-08-29-tndrop-v1.7-text-edit-design.md` (§参照はこの文書)。

## Global Constraints

- ソースは UTF-8 (BOM なし) LF。新規 NuGet 禁止。オンライン依存禁止 (CLAUDE.md §2.2)。
- 文言は必ず 3 点セット (Strings.resx ja / Strings.en.resx / Strings.cs プロパティ)。StringsResxParityTests が整合を強制。
- XAML コメントに `--` を書かない (単一の `-`)。
- ダークパレット必須: 新規ダイアログの全テキストに明示 Foreground、全フォーカス可能コントロールに FocusVisual (v1.4/v1.6 で 2 度再発した既知の欠陥クラス)。
- 変異メソッドは内部で Save() しない。呼び出し側が直後に `Save()` を呼ぶ (ShelfWindow.OnCardActionClick と同じ作法・同じ理由コメント)。`Changed` はロックの外で発火。
- 保存時の 3 項目 (Text / Kind / ContentHash) は `UpdateText` 以外の場所で個別に更新しない。
- コミットメッセージ末尾: `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>`。各タスク完了時に `dotnet test` 全 green。

---

### Task 1: ItemStore.UpdateText

**Files:**
- Modify: `src/TNDrop.App/Core/ItemStore.cs` (`MoveToTop` (206-220 行) の直後に挿入)
- Test: `src/TNDrop.Tests/ItemStoreUpdateTextTests.cs` (新規)

**Interfaces:**
- Consumes: 既存 `UrlDetector.IsUrl(string)` (TNDrop.Core, UrlDetector.cs:5) / `ItemStore.Fnv1a(ReadOnlySpan<byte>)` (public static, ItemStore.cs:967) / `_lock` / `_items` / `Changed`
- Produces: `public bool UpdateText(string id, string newText)` — Task 2 の EditDialog が呼ぶ。true = 更新した (呼び出し側が続けて `Save()` を呼ぶ)。false = 対象が見つからない or 種別対象外 (何もしない)。空白のみの newText は `ArgumentException`。

- [ ] **Step 1: 失敗するテストを書く**

`src/TNDrop.Tests/ItemStoreUpdateTextTests.cs`:

```csharp
using System.Text;
using TNDrop.Core;

public class ItemStoreUpdateTextTests : IDisposable
{
    private readonly string _dir = Path.Combine(Path.GetTempPath(), "tndrop-test-" + Guid.NewGuid());

    public void Dispose()
    {
        try { Directory.Delete(_dir, true); } catch { }
    }

    private static ClipItem NewText(string text) => new()
    {
        Kind = ClipKind.Text,
        Text = text,
        CreatedAtUtc = DateTime.UtcNow,
        ContentHash = ItemStore.Fnv1a(Encoding.UTF8.GetBytes(text)),
    };

    [Fact]
    public void UpdateText_TextStaysText_UpdatesTextAndHash()
    {
        var store = new ItemStore(_dir);
        var item = NewText("before");
        store.TryAdd(item);

        var ok = store.UpdateText(item.Id, "after");

        Assert.True(ok);
        var updated = store.Items.Single(i => i.Id == item.Id);
        Assert.Equal("after", updated.Text);
        Assert.Equal(ClipKind.Text, updated.Kind);
        Assert.Equal(ItemStore.Fnv1a(Encoding.UTF8.GetBytes("after")), updated.ContentHash);
    }

    [Fact]
    public void UpdateText_TextBecomesUrl_ReclassifiesToLink()
    {
        // 先頭の h が欠けた URL に h を足すユースケース (設計書 §2 の例そのもの)
        var store = new ItemStore(_dir);
        var item = NewText("ttps://example.com/page");
        store.TryAdd(item);

        var ok = store.UpdateText(item.Id, "https://example.com/page");

        Assert.True(ok);
        var updated = store.Items.Single(i => i.Id == item.Id);
        Assert.Equal(ClipKind.Link, updated.Kind);
        Assert.Equal("https://example.com/page", updated.Text);
        Assert.Equal(ItemStore.Fnv1a(Encoding.UTF8.GetBytes("https://example.com/page")), updated.ContentHash);
    }

    [Fact]
    public void UpdateText_LinkBecomesText_ReclassifiesToText()
    {
        var store = new ItemStore(_dir);
        var item = new ClipItem
        {
            Kind = ClipKind.Link,
            Text = "https://example.com/",
            CreatedAtUtc = DateTime.UtcNow,
            ContentHash = ItemStore.Fnv1a(Encoding.UTF8.GetBytes("https://example.com/")),
        };
        store.TryAdd(item);

        var ok = store.UpdateText(item.Id, "ただのメモ");

        Assert.True(ok);
        Assert.Equal(ClipKind.Text, store.Items.Single(i => i.Id == item.Id).Kind);
    }

    [Fact]
    public void UpdateText_PreservesIdCreatedAtAndPinned()
    {
        var store = new ItemStore(_dir);
        var created = new DateTime(2026, 1, 2, 3, 4, 5, DateTimeKind.Utc);
        var item = NewText("before");
        item.CreatedAtUtc = created;
        store.TryAdd(item);
        store.SetPinned(item.Id, true);

        store.UpdateText(item.Id, "after");

        var updated = store.Items.Single(i => i.Id == item.Id);
        Assert.Equal(item.Id, updated.Id);
        Assert.Equal(created, updated.CreatedAtUtc);
        Assert.True(updated.Pinned);
    }

    [Fact]
    public void UpdateText_UnknownId_ReturnsFalse_NoChange()
    {
        var store = new ItemStore(_dir);
        var item = NewText("keep");
        store.TryAdd(item);

        Assert.False(store.UpdateText("no-such-id", "x"));
        Assert.Equal("keep", store.Items.Single().Text);
    }

    [Fact]
    public void UpdateText_NonTextKinds_ReturnsFalse_NoChange()
    {
        var store = new ItemStore(_dir);
        var files = new ClipItem
        {
            Kind = ClipKind.Files,
            Paths = new List<string> { @"C:\a.txt" },
            CreatedAtUtc = DateTime.UtcNow,
            ContentHash = ItemStore.Fnv1a(Encoding.UTF8.GetBytes(@"C:\a.txt")),
        };
        store.TryAdd(files);

        Assert.False(store.UpdateText(files.Id, "x"));
        Assert.Equal(ClipKind.Files, store.Items.Single().Kind);
    }

    [Theory]
    [InlineData("")]
    [InlineData("   ")]
    [InlineData(null)]
    public void UpdateText_BlankText_Throws(string? blank)
    {
        var store = new ItemStore(_dir);
        var item = NewText("keep");
        store.TryAdd(item);

        Assert.Throws<ArgumentException>(() => store.UpdateText(item.Id, blank!));
        Assert.Equal("keep", store.Items.Single().Text);
    }

    [Fact]
    public void UpdateText_RaisesChanged()
    {
        var store = new ItemStore(_dir);
        var item = NewText("before");
        store.TryAdd(item);
        var changed = 0;
        store.Changed += () => changed++;

        store.UpdateText(item.Id, "after");

        Assert.Equal(1, changed);
    }

    [Fact]
    public void UpdateText_ThenSave_PersistsAcrossReload()
    {
        var store = new ItemStore(_dir);
        var item = NewText("before");
        store.TryAdd(item);
        store.UpdateText(item.Id, "https://example.com/edited");
        store.Save();

        var reloaded = new ItemStore(_dir);
        reloaded.Load();
        var loaded = reloaded.Items.Single(i => i.Id == item.Id);
        Assert.Equal("https://example.com/edited", loaded.Text);
        Assert.Equal(ClipKind.Link, loaded.Kind);
    }
}
```

注意: `TryAdd` は先頭アイテムと同一 ContentHash を重複拒否する (ItemStore.cs:107)。上のテストは各ストアに異なる内容しか入れないので影響しない。`Items` / `Changed` / `TryAdd` の実名は v1.6 のテスト群 (ItemStoreBackupApiTests) で使用実績あり。

- [ ] **Step 2: 実行して失敗を確認**

Run: `dotnet test --filter ItemStoreUpdateTextTests`
Expected: コンパイルエラー (UpdateText 未定義) で FAIL

- [ ] **Step 3: 実装**

`src/TNDrop.App/Core/ItemStore.cs` の `MoveToTop` (220 行) の直後に挿入:

```csharp
/// <summary>
/// Saves an edit made in the EditDialog (v1.7). Updates the THREE fields that must stay
/// consistent with each other in ONE resolution: Text (the new content), Kind (re-classified
/// with the same <see cref="UrlDetector.IsUrl"/> the capture paths use, so an edited URL
/// becomes a Link card and vice versa), and ContentHash (same Fnv1a-of-UTF8 derivation as
/// CapturePipeline). Deciding any of these elsewhere would let them silently disagree.
/// Id / CreatedAtUtc / Pinned are deliberately untouched (an edit is a correction, not a
/// new capture). Does NOT call Save() itself -- like SetPinned/MoveToTop, the caller
/// persists right after. Returns false (and changes nothing) when the id is unknown or the
/// item is not a Text/Link card; the UI never offers Edit on other kinds, so false there
/// means the card was deleted while the dialog was open.
/// </summary>
public bool UpdateText(string id, string newText)
{
    if (string.IsNullOrWhiteSpace(newText))
    {
        // The dialog disables Save on blank input; this is second-line defence so a
        // programming error can never blank out a card's content.
        throw new ArgumentException("edited text must not be blank", nameof(newText));
    }

    var updated = false;
    lock (_lock)
    {
        var item = _items.FirstOrDefault(i => i.Id == id);
        if (item is { Kind: ClipKind.Text or ClipKind.Link })
        {
            item.Text = newText;
            item.Kind = UrlDetector.IsUrl(newText) ? ClipKind.Link : ClipKind.Text;
            item.ContentHash = Fnv1a(Encoding.UTF8.GetBytes(newText));
            updated = true;
        }
    }

    if (updated)
    {
        Changed?.Invoke();
    }

    return updated;
}
```

(`using System.Text;` はファイル冒頭に既にある — Fnv1a 呼び出し箇所多数のため。無ければ追加。)

- [ ] **Step 4: テストが通ることを確認**

Run: `dotnet test --filter ItemStoreUpdateTextTests`
Expected: PASS (11 件)。続けて `dotnet test` で既存含め全 green。

- [ ] **Step 5: Commit**

```bash
git add src/TNDrop.App/Core/ItemStore.cs src/TNDrop.Tests/ItemStoreUpdateTextTests.cs
git commit -m "feat: ItemStore.UpdateText with kind/hash re-resolution (v1.7 Task 1)"
```

---

### Task 2: EditDialog + App 単一インスタンス管理 + 文言

**Files:**
- Create: `src/TNDrop.App/UI/EditDialog.xaml` / `EditDialog.xaml.cs`
- Modify: `src/TNDrop.App/App.xaml.cs` (`_backupDialog` フィールド (45 行) と `OnBackupDialogRequested` (1296-1314 行) / `OpenBackupDialog` (1323-1329 行) の近く)
- Modify: `src/TNDrop.App/Resources/Strings.cs` / `Strings.resx` / `Strings.en.resx`

**Interfaces:**
- Consumes: Task 1 `ItemStore.UpdateText` / `App.Store` (public static) / `Store.Save()`
- Produces: `public static void App.OpenEditDialog(string itemId, string currentText)` — Task 3 の ShelfWindow が呼ぶ。単一インスタンス: 既に開いていれば**前面化のみ** (ターゲット差し替えなし、設計書 §4)。

- [ ] **Step 1: 文言追加 (3 点セット)**

設計書 §6 の表のとおり。resx の `<data name="..." xml:space="preserve"><value>...</value></data>` 形式は既存エントリに合わせ、`Strings.cs` は `public static string Xxx => Get(nameof(Xxx));` 形式:

| キー | ja | en |
|---|---|---|
| `ActionEdit` | 編集 | Edit |
| `EditDialogTitle` | テキストの編集 | Edit text |
| `EditSaveButton` | 保存(_S) | Save(_S) |
| `EditCancelButton` | キャンセル | Cancel |
| `EditItemGone` | このカードは既に削除されています。 | This card has already been deleted. |

- [ ] **Step 2: EditDialog.xaml を作る**

PasswordDialog.xaml (全 157 行) を開いて流儀を踏襲する。`Dlg.*` リソース (Foreground/SubForeground/Border/HoverBorder/FieldBackground)、`Dlg.FocusVisual`、`Dlg.Button` は**共有辞書ではなく各ダイアログのローカル定義**がこのプロジェクトの確立した慣習なので、PasswordDialog.xaml の該当ブロック (19-49 行、106-138 行) を EditDialog.xaml の `Window.Resources` にコピーして使う (キー名も同じ `Dlg.*` でよい — スコープがウインドウ内なので衝突しない)。

ウインドウ属性: `Title` はコードで設定、`Width="480" Height="360"`, `WindowStartupLocation="CenterScreen"`, `ShowInTaskbar="False"`, `ResizeMode="CanResize"` (長文編集のため可変)、背景・クロムは PasswordDialog と同じ。

本体レイアウト (Grid 2 行):

```xml
<Grid Margin="12">
    <Grid.RowDefinitions>
        <RowDefinition Height="*" />
        <RowDefinition Height="Auto" />
    </Grid.RowDefinitions>
    <TextBox x:Name="BodyTextBox"
             Grid.Row="0"
             AcceptsReturn="True"
             AcceptsTab="False"
             TextWrapping="Wrap"
             VerticalScrollBarVisibility="Auto"
             FontSize="13"
             Padding="8"
             Background="{StaticResource Dlg.FieldBackground}"
             Foreground="{StaticResource Dlg.Foreground}"
             BorderBrush="{StaticResource Dlg.Border}"
             CaretBrush="{StaticResource Dlg.Foreground}"
             FocusVisualStyle="{StaticResource Dlg.FocusVisual}"
             TextChanged="OnBodyTextChanged" />
    <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
        <Button x:Name="SaveButton" Style="{StaticResource Dlg.Button}" MinWidth="96"
                IsDefault="False" Click="OnSaveClick" />
        <Button x:Name="CancelButton" Style="{StaticResource Dlg.Button}" MinWidth="96" Margin="8,0,0,0"
                IsCancel="True" />
    </StackPanel>
</Grid>
```

(`IsDefault` を付けない理由: 複数行編集で Enter は改行に使うため。`IsCancel="True"` で Esc で閉じる。XAML コメントを書く場合は `--` を含めないこと。)

- [ ] **Step 3: EditDialog.xaml.cs を書く**

```csharp
using System;
using System.Windows;
using TNDrop.Resources;

namespace TNDrop.UI;

/// <summary>
/// v1.7 のテキスト編集ダイアログ。1 枚のカード (Text/Link) の本文を編集して保存する。
/// 保存の意味論 (本文・種別・ハッシュの同時更新) は ItemStore.UpdateText が唯一の入口で、
/// このクラスは入力と結果表示だけを担当する。単一インスタンス管理は App 側 (OpenEditDialog)。
/// </summary>
public sealed partial class EditDialog : Window
{
    private readonly string _itemId;

    public EditDialog(string itemId, string currentText)
    {
        InitializeComponent();
        _itemId = itemId;

        Title = Strings.EditDialogTitle;
        SaveButton.Content = Strings.EditSaveButton;
        CancelButton.Content = Strings.EditCancelButton;

        BodyTextBox.Text = currentText;
        UpdateSaveEnabled();

        // 開いたらすぐ打てるように。SelectAll はしない (うっかり全置換を防ぐ)。
        Loaded += (_, _) => { BodyTextBox.Focus(); BodyTextBox.CaretIndex = BodyTextBox.Text.Length; };
    }

    private void OnBodyTextChanged(object sender, RoutedEventArgs e) => UpdateSaveEnabled();

    private void UpdateSaveEnabled() =>
        SaveButton.IsEnabled = !string.IsNullOrWhiteSpace(BodyTextBox.Text);

    private void OnSaveClick(object sender, RoutedEventArgs e)
    {
        var store = global::TNDrop.App.Store;
        if (store is null)
        {
            Close();
            return;
        }

        if (store.UpdateText(_itemId, BodyTextBox.Text))
        {
            // 変異メソッドは内部で Save しない作法 (SetPinned と同じ)。編集はこの後に
            // 別の保存点が無いので、ここで即座に永続化する (OnCardActionClick と同じ理由)。
            store.Save();
            Close();
        }
        else
        {
            // 編集中にカードが削除された (手動削除・自動削除・リストア)。設計書 §4。
            MessageBox.Show(this, Strings.EditItemGone, Strings.EditDialogTitle,
                MessageBoxButton.OK, MessageBoxImage.Information);
            Close();
        }
    }
}
```

- [ ] **Step 4: App.xaml.cs に単一インスタンス管理を追加**

`_backupDialog` フィールド (App.xaml.cs:45) の隣に `private EditDialog? _editDialog;` を追加し、`OnBackupDialogRequested` (1296-1314 行) / `OpenBackupDialog` (1323-1329 行) と同型で:

```csharp
/// <summary>ShelfWindow のカード編集ボタンから呼ぶ (v1.7)。単一インスタンス:
/// 既に開いている場合は前面化のみで、ターゲットは差し替えない (未保存の編集内容を
/// 黙って捨てないため — 設計書 §4)。</summary>
public static void OpenEditDialog(string itemId, string currentText)
{
    if (System.Windows.Application.Current is App app)
    {
        app.OnEditDialogRequested(itemId, currentText);
    }
}

private void OnEditDialogRequested(string itemId, string currentText)
{
    if (_editDialog is null)
    {
        _editDialog = new EditDialog(itemId, currentText);
        _editDialog.Closed += (_, _) => _editDialog = null;
    }

    if (_editDialog.WindowState == WindowState.Minimized)
    {
        _editDialog.WindowState = WindowState.Normal;
    }

    _editDialog.Show();
    _editDialog.Activate();
}
```

- [ ] **Step 5: ビルド・全テスト**

Run: `dotnet build` → clean (新規警告なし)、`dotnet test` → 全 green (パリティテスト含む)。この時点で UI からの入口はまだ無い (Task 3)。

- [ ] **Step 6: Commit**

```bash
git add src/TNDrop.App/UI/EditDialog.xaml src/TNDrop.App/UI/EditDialog.xaml.cs src/TNDrop.App/App.xaml.cs src/TNDrop.App/Resources/Strings.cs src/TNDrop.App/Resources/Strings.resx src/TNDrop.App/Resources/Strings.en.resx
git commit -m "feat: edit dialog and single-instance wiring (v1.7 Task 2)"
```

---

### Task 3: カードの編集ボタン (Cards.xaml + ShelfWindow)

**Files:**
- Modify: `src/TNDrop.App/UI/Cards.xaml` (ActionPill 内、493-502 行付近)
- Modify: `src/TNDrop.App/UI/ShelfWindow.xaml.cs` (`OnCardActionClick`, 956-984 行)

**Interfaces:**
- Consumes: Task 2 `App.OpenEditDialog(string itemId, string currentText)` / `CardViewModel.Id` (string) / `CardViewModel.Kind` (ClipKind) / `CardViewModel.Item` (ClipItem — 生テキストは `card.Item.Text`。`CardViewModel.Title` は表示用トリム済みなので使わない)
- Produces: なし (末端 UI)

- [ ] **Step 1: Cards.xaml に編集ボタンを追加**

ActionPill 内の並びを Pin → **Edit** → 区切り → Delete にする。既存 (493-502 行):

```xml
<Button Tag="Pin" Style="{StaticResource Card.PinActionButtonStyle}" Margin="0" />
<Border Width="1" Margin="5,4" Background="{StaticResource Card.PillSeparator}" />
<Button Content="&#128465;" Tag="Delete" Style="{StaticResource Card.ActionButton}" Margin="0" ... />
```

Pin ボタンの直後 (区切りの前) に挿入:

```xml
<!-- Edit action (v1.7): only Text/Link cards can be edited, so the button collapses on
     Image/Files cards instead of being disabled - a disabled button would still widen the
     pill and invite "why is this greyed out" on cards where editing has no meaning. -->
<Button Content="&#9998;" Tag="Edit" Margin="0"
        ToolTip="{x:Static res:Strings.ActionEdit}"
        AutomationProperties.Name="{x:Static res:Strings.ActionEdit}">
    <Button.Style>
        <Style TargetType="Button" BasedOn="{StaticResource Card.ActionButton}">
            <Setter Property="Visibility" Value="Collapsed" />
            <Style.Triggers>
                <DataTrigger Binding="{Binding Kind}" Value="Text">
                    <Setter Property="Visibility" Value="Visible" />
                </DataTrigger>
                <DataTrigger Binding="{Binding Kind}" Value="Link">
                    <Setter Property="Visibility" Value="Visible" />
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </Button.Style>
</Button>
```

(`&#9998;` = ✎。`CardViewModel.Kind` は `ClipKind` 型プロパティで、DataTrigger の Value="Text" は enum 名の文字列比較で機能する — 既存 Cards.xaml の Kind ベース DataTrigger (545-556 行) と同じ流儀。XAML コメントに `--` を書かないこと。)

- [ ] **Step 2: ShelfWindow.OnCardActionClick に case を追加**

`OnCardActionClick` (956-984 行) の switch に、既存 2 case の前に追加:

```csharp
case "Edit":
    // Opens (or fronts) the editor; nothing in the store changed yet, so skip the
    // unconditional Save() below - it exists for Pin/Delete which mutate immediately.
    global::TNDrop.App.OpenEditDialog(card.Id, card.Item.Text ?? "");
    e.Handled = true;
    return;
```

(早期 return で既存の `_itemStore.Save()` を通らないこと。Pin/Delete の既存動作は一切変えない。)

- [ ] **Step 3: ビルド・全テスト・手動スモーク**

Run: `dotnet build` clean、`dotnet test` 全 green。

手動スモーク: 開発機でユーザーの本物の TNDrop が起動中 (単一インスタンス Mutex) の場合は起動できないので、その場合はスモーク不可と報告に明記し (v1.6 と同じ扱い)、実機チェックリスト §32 に委ねる。起動できる場合: テキストカードに ✎ が出る / 画像・ファイルカードに出ない / ✎ → ダイアログ → 編集 → 保存でカード表示が変わる / `ttps://` に h を足すとリンクカード化、を確認して終了する。

- [ ] **Step 4: Commit**

```bash
git add src/TNDrop.App/UI/Cards.xaml src/TNDrop.App/UI/ShelfWindow.xaml.cs
git commit -m "feat: edit action button on text/link cards (v1.7 Task 3)"
```

---

### Task 4: ドキュメント・バージョン・最終検証

**Files:**
- Modify: `docs/manual-test-checklist.md` (§31 の表末尾 382 行の直後、`---` (384 行) の前に §32 を挿入)
- Modify: `assets/README.html` (バージョン 3 箇所: 7 行 title / 935 行 nav / 1624 行 foot。機能説明を v1.6 節の近くに追記)
- Modify: `src/TNDrop.App/TNDrop.App.csproj:13` (`<Version>1.7.0</Version>`)
- Modify: `installer/setup.iss:6` (`#define MyAppVersion    "1.7.0"` — 他は変更禁止、AppId 不変)
- Modify: `CLAUDE.md` (§9 実装計画リスト末尾に `docs/superpowers/plans/2026-08-29-tndrop-v1.7-text-edit.md (v1.7)` を追記)

**Interfaces:** なし (ドキュメントのみ)

- [ ] **Step 1: manual-test-checklist.md に §32 を追加**

§31 の表の直後 (382 行の後、384 行の `---` の前) に、§30/§31 と同じ形式で:

```markdown

## 32. v1.7 追加機能 (テキスト編集)

| # | 操作 | 期待結果 | 結果 |
|---|------|----------|------|
| 32-1 | テキストカードにマウスを乗せる | ピンと削除の間に ✎ (編集) が出る。ツールチップ「編集」 | |
| 32-2 | 画像・ファイル・スタックカードにマウスを乗せる | ✎ が出ない | |
| 32-3 | [必須] ✎ → 本文を書き換えて保存 | カード表示が即時更新される (再起動不要)。位置・日時・ピン状態は変わらない | |
| 32-4 | [必須] `ttps://…` のテキストカードに先頭の h を足して保存 | リンクカードに変わり「開く」ホットスポットが効く。リンクタブに移る | |
| 32-5 | リンクカードの URL を文章に書き換えて保存 | テキストカードに戻る | |
| 32-6 | 編集ダイアログを開いたままシェルフを格納させる | 編集は継続でき、保存も効く | |
| 32-7 | 編集中に対象カードを削除 → 保存 | 「このカードは既に削除されています。」が出てダイアログが閉じる。他のカードは無傷 | |
| 32-8 | 編集ダイアログを開いたまま別カードの ✎ を押す | 既存のダイアログが前面化するだけ (内容は差し替わらない) | |
| 32-9 | 本文を全部消す | 保存ボタンが無効になる | |
| 32-10 | 編集して保存 → カードをクリック | 編集後のテキストがコピーされる | |
```

- [ ] **Step 2: README.html 更新**

v1.6 節の体裁に合わせて編集機能の説明を追記 (✎ の場所、保存で種別が自動で切り替わること、保存のみでクリップボードには入らないこと)。バージョン表記 3 箇所 (7/935/1624 行 — 行番号はずれている可能性があるので "1.6.0" を検索して全置換し、置換後に "1.6.0" が 0 件であることを確認) を v1.7.0 に。

- [ ] **Step 3: バージョンと CLAUDE.md**

csproj / setup.iss を 1.7.0 に。CLAUDE.md §9 の実装計画リスト末尾に本計画のポインタを追記。

- [ ] **Step 4: 最終検証**

Run: `dotnet test` → 全 green (既存 496 + 本計画 11 = 507 目安)
Run: `powershell -File build.ps1` → test → publish → ISCC が通り `dist\TNDrop-Setup-1.7.0.exe` ができること

- [ ] **Step 5: Commit**

```bash
git add docs/manual-test-checklist.md assets/README.html src/TNDrop.App/TNDrop.App.csproj installer/setup.iss CLAUDE.md
git commit -m "docs: v1.7 checklist section 32, README, version bump to 1.7.0"
```

---

## 実装順序と依存

```
Task 1 (ItemStore.UpdateText) → Task 2 (EditDialog + App) → Task 3 (カードボタン) → Task 4 (docs/version)
```

直列。Task 1 だけは独立して先行可能。

## 実機テストゲート (リリース前・利用者実施)

§32 の [必須] 2 項目 (32-3 編集反映、32-4 リンク化) を含む一式は開発機の自動テストでは代替できない (特に開発機で GUI スモークができなかった場合)。リリース判断の前に実機で消化すること。
