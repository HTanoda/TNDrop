# TNDrop v1.6 バックアップ・リストア・別端末移行 実装計画

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** データ一式 (items.dat + blobs + settings.json) の手動/自動バックアップ・リストアと、パスワード暗号化ファイルによる別端末移行を実装する。あわせて格納遅延スライダーの下限を 100ms に下げる。

**Architecture:** Core に純粋ロジック 3 クラス (BackupPruning / ExportContainer / BackupService) を新設し、ItemStore にファイル差し替え用の最小 API を追加する。UI は BackupDialog (新規) と SettingsWindow / TrayIcon への小改修。復元は「退避 → 検証 → 差し替え → 再読込 → 失敗時巻き戻し」の 1 本の共通フローに統一し、インポートは「.tndexport を復号して同じフローに渡す」だけにする。

**Tech Stack:** .NET 10 / WPF、System.IO.Compression (ZipFile/ZipArchive)、System.Security.Cryptography (Aes, Rfc2898DeriveBytes, HMACSHA256, ProtectedData)、xunit + Xunit.StaFact。**追加 NuGet なし** (すべて BCL)。

**正とする設計書:** `docs/superpowers/specs/2026-08-29-tndrop-v1.6-backup-migration-design.md` (§番号の参照はすべてこの文書)。

## Global Constraints

- 本番機はオフライン。CDN / 外部 API / 実行時ダウンロードを書かない (CLAUDE.md §2.2)。
- アプリ本体の NuGet は System.Security.Cryptography.ProtectedData のみ。新規依存を追加しない。
- ソースは UTF-8 (BOM なし) LF。resx は既存ファイルの形式に従う。日本語ファイル名を配布物に入れない。
- 文言は必ず 3 点セットで追加: `Strings.resx` (ja) + `Strings.en.resx` (en) + `Strings.cs` のプロパティ。パリティテスト (`StringsResxParityTests`) があるため 1 つでも欠けるとテストが落ちる。
- エンドユーザーにスタックトレースを見せない。失敗は「何に失敗したか + 次の行動」+ ログ (CLAUDE.md §6)。
- ログは `FileLogger.Instance?.Info/Warn/Error(module, message)`。パスワード・履歴内容をログに出さない。
- XML 系ファイル (.xaml/.csproj) のコメントに `--` を書かない (単一の `-` を使う)。
- コミットメッセージ末尾: `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>`。
- 各タスク完了時に `dotnet test` が green であること。

---

### Task 1: BackupPruning (世代刈り込みの純関数)

**Files:**
- Create: `src/TNDrop.App/Core/BackupPruning.cs`
- Test: `src/TNDrop.Tests/BackupPruningTests.cs`

**Interfaces:**
- Consumes: なし (純関数)
- Produces: `BackupKind` enum と `BackupPruning.SelectFilesToDelete(IEnumerable<string> fileNames)` → `IReadOnlyList<string>`。Task 5 の BackupService が保存後に呼ぶ。

- [ ] **Step 1: 失敗するテストを書く**

`src/TNDrop.Tests/BackupPruningTests.cs`:

```csharp
using TNDrop.Core;

public class BackupPruningTests
{
    [Fact]
    public void Auto_KeepsNewestSeven_DeletesOlder()
    {
        var files = Enumerable.Range(1, 9)
            .Select(d => $"auto-2026080{d}.zip")
            .ToList();

        var doomed = BackupPruning.SelectFilesToDelete(files);

        Assert.Equal(new[] { "auto-20260801.zip", "auto-20260802.zip" }, doomed.OrderBy(x => x));
    }

    [Fact]
    public void Auto_ExactlySeven_DeletesNothing()
    {
        var files = Enumerable.Range(1, 7).Select(d => $"auto-2026080{d}.zip");
        Assert.Empty(BackupPruning.SelectFilesToDelete(files));
    }

    [Fact]
    public void Safety_KeepsNewestThree()
    {
        var files = new[]
        {
            "safety-20260801-090000.zip",
            "safety-20260802-090000.zip",
            "safety-20260803-090000.zip",
            "safety-20260804-090000.zip",
        };
        Assert.Equal(new[] { "safety-20260801-090000.zip" }, BackupPruning.SelectFilesToDelete(files));
    }

    [Fact]
    public void Manual_NeverDeleted()
    {
        var files = Enumerable.Range(1, 30).Select(d => $"manual-202608{d:00}-120000.zip");
        Assert.Empty(BackupPruning.SelectFilesToDelete(files));
    }

    [Fact]
    public void MixedKinds_PrunedIndependently_UnknownNamesIgnored()
    {
        var files = new List<string> { "readme.txt", "desktop.ini" };
        files.AddRange(Enumerable.Range(1, 8).Select(d => $"auto-2026080{d}.zip"));
        files.AddRange(Enumerable.Range(1, 4).Select(d => $"safety-2026080{d}-090000.zip"));
        files.Add("manual-20260801-120000.zip");

        var doomed = BackupPruning.SelectFilesToDelete(files);

        Assert.Equal(
            new[] { "auto-20260801.zip", "safety-20260801-090000.zip" },
            doomed.OrderBy(x => x));
    }

    [Fact]
    public void Empty_ReturnsEmpty()
    {
        Assert.Empty(BackupPruning.SelectFilesToDelete(Array.Empty<string>()));
    }
}
```

- [ ] **Step 2: 実行して失敗を確認**

Run: `dotnet test --filter BackupPruningTests`
Expected: コンパイルエラー (BackupPruning 未定義) で FAIL

- [ ] **Step 3: 実装**

`src/TNDrop.App/Core/BackupPruning.cs`:

```csharp
using System;
using System.Collections.Generic;
using System.Linq;

namespace TNDrop.Core;

/// <summary>バックアップ ZIP の種別。ファイル名プレフィックスと manifest の kind に対応する。</summary>
public enum BackupKind { Auto, Manual, Safety }

/// <summary>
/// backups フォルダの世代刈り込み規則 (設計書 §3)。auto は新しい順 7 件、safety は 3 件を
/// 保持し、manual と規則外の名前は決して削除対象にしない。ファイル名は
/// auto-YYYYMMDD.zip / safety-YYYYMMDD-HHmmss.zip の形式で、辞書順 = 時系列順に
/// なるよう設計されているため、ソートに更新日時ではなくファイル名を使う (テスト容易性と、
/// コピーで復元されたファイルの更新日時が当てにならないため)。
/// </summary>
public static class BackupPruning
{
    public const int AutoKeep = 7;
    public const int SafetyKeep = 3;

    /// <summary>削除すべきファイル名 (パスではなく名前) を返す。呼び出し側が実削除する。</summary>
    public static IReadOnlyList<string> SelectFilesToDelete(IEnumerable<string> fileNames)
    {
        var names = fileNames.ToList();
        var doomed = new List<string>();
        doomed.AddRange(SelectOverflow(names, "auto-", AutoKeep));
        doomed.AddRange(SelectOverflow(names, "safety-", SafetyKeep));
        return doomed;
    }

    private static IEnumerable<string> SelectOverflow(List<string> names, string prefix, int keep) =>
        names
            .Where(n => n.StartsWith(prefix, StringComparison.OrdinalIgnoreCase)
                        && n.EndsWith(".zip", StringComparison.OrdinalIgnoreCase))
            .OrderByDescending(n => n, StringComparer.OrdinalIgnoreCase)
            .Skip(keep);
}
```

- [ ] **Step 4: テストが通ることを確認**

Run: `dotnet test --filter BackupPruningTests`
Expected: PASS (6 件)

- [ ] **Step 5: Commit**

```bash
git add src/TNDrop.App/Core/BackupPruning.cs src/TNDrop.Tests/BackupPruningTests.cs
git commit -m "feat: add BackupPruning generation rules (v1.6 Task 1)"
```

---

### Task 2: ExportContainer (.tndexport 暗号コンテナ)

**Files:**
- Create: `src/TNDrop.App/Core/ExportContainer.cs`
- Test: `src/TNDrop.Tests/ExportContainerTests.cs`

**Interfaces:**
- Consumes: なし
- Produces: 設計書 §6.3 の形式を実装する static クラス:
  - `ExportContainer.MinPasswordLength` = 8 (const int)
  - `ExportContainer.Encrypt(byte[] plaintext, string password)` → `byte[]`
  - `ExportContainer.Decrypt(byte[] container, string password)` → `byte[]` (失敗は例外)
  - 例外型 `ExportFormatException` (magic/version 不一致・短すぎる入力) と `ExportPasswordException` (HMAC 不一致 = パスワード誤り or 改ざん)。Task 5 と Task 7 が catch して文言を出し分ける。

- [ ] **Step 1: 失敗するテストを書く**

`src/TNDrop.Tests/ExportContainerTests.cs`:

```csharp
using System.Text;
using TNDrop.Core;

public class ExportContainerTests
{
    private static readonly byte[] Payload = Encoding.UTF8.GetBytes("{\"hello\":\"世界\"}");

    [Fact]
    public void RoundTrip_ReturnsOriginalBytes()
    {
        var container = ExportContainer.Encrypt(Payload, "correct horse");
        var back = ExportContainer.Decrypt(container, "correct horse");
        Assert.Equal(Payload, back);
    }

    [Fact]
    public void Encrypt_ProducesDifferentBytesEachTime()
    {
        // salt/IV が毎回ランダムであることの確認 (固定だと同じ平文が同じ暗号文になる)
        var a = ExportContainer.Encrypt(Payload, "pw12345678");
        var b = ExportContainer.Encrypt(Payload, "pw12345678");
        Assert.NotEqual(a, b);
    }

    [Fact]
    public void WrongPassword_ThrowsPasswordException()
    {
        var container = ExportContainer.Encrypt(Payload, "correct horse");
        Assert.Throws<ExportPasswordException>(() => ExportContainer.Decrypt(container, "wrong horse"));
    }

    [Fact]
    public void TamperedByte_ThrowsPasswordException()
    {
        var container = ExportContainer.Encrypt(Payload, "correct horse");
        container[container.Length / 2] ^= 0xFF;
        Assert.Throws<ExportPasswordException>(() => ExportContainer.Decrypt(container, "correct horse"));
    }

    [Fact]
    public void WrongMagic_ThrowsFormatException()
    {
        var container = ExportContainer.Encrypt(Payload, "correct horse");
        container[0] = (byte)'X';
        Assert.Throws<ExportFormatException>(() => ExportContainer.Decrypt(container, "correct horse"));
    }

    [Fact]
    public void UnknownVersion_ThrowsFormatException()
    {
        var container = ExportContainer.Encrypt(Payload, "correct horse");
        container[4] = 0x63;
        Assert.Throws<ExportFormatException>(() => ExportContainer.Decrypt(container, "correct horse"));
    }

    [Fact]
    public void TooShortInput_ThrowsFormatException()
    {
        Assert.Throws<ExportFormatException>(() => ExportContainer.Decrypt(new byte[10], "correct horse"));
    }

    [Fact]
    public void MinPasswordLength_IsEight()
    {
        Assert.Equal(8, ExportContainer.MinPasswordLength);
    }
}
```

- [ ] **Step 2: 実行して失敗を確認**

Run: `dotnet test --filter ExportContainerTests`
Expected: コンパイルエラーで FAIL

- [ ] **Step 3: 実装**

`src/TNDrop.App/Core/ExportContainer.cs`:

```csharp
using System;
using System.IO;
using System.Security.Cryptography;
using System.Text;

namespace TNDrop.Core;

/// <summary>.tndexport のフォーマットエラー (TNDrop のエクスポートファイルではない)。</summary>
public sealed class ExportFormatException : Exception
{
    public ExportFormatException(string message) : base(message) { }
}

/// <summary>HMAC 不一致 = パスワード誤りまたは改ざん。どちらかは区別できない (設計書 §6.2)。</summary>
public sealed class ExportPasswordException : Exception
{
    public ExportPasswordException(string message) : base(message) { }
}

/// <summary>
/// 別端末移行ファイル (.tndexport) の暗号コンテナ (設計書 §6.3, format 1)。
/// レイアウト: magic "TNDX" (4B) | version (1B=0x01) | salt (16B) | IV (16B)
/// | ciphertext (AES-256-CBC, PKCS7) | HMAC-SHA256 (32B, 先頭から ciphertext 末尾まで)。
/// 鍵導出: PBKDF2-SHA256 600,000 回で 64B → 前半 32B が AES 鍵、後半 32B が HMAC 鍵。
/// encrypt-then-MAC: 復号よりも先に HMAC を検証するので、細工された暗号文が
/// AES/PKCS7 のエラー経路に到達しない。
/// </summary>
public static class ExportContainer
{
    public const int MinPasswordLength = 8;
    public const byte FormatVersion = 0x01;

    private static readonly byte[] Magic = Encoding.ASCII.GetBytes("TNDX");
    private const int SaltSize = 16;
    private const int IvSize = 16;
    private const int MacSize = 32;
    private const int Pbkdf2Iterations = 600_000;
    private const int HeaderSize = 4 + 1 + SaltSize + IvSize;

    public static byte[] Encrypt(byte[] plaintext, string password)
    {
        var salt = RandomNumberGenerator.GetBytes(SaltSize);
        var iv = RandomNumberGenerator.GetBytes(IvSize);
        var (aesKey, macKey) = DeriveKeys(password, salt);

        using var aes = Aes.Create();
        aes.Key = aesKey;
        var ciphertext = aes.EncryptCbc(plaintext, iv, PaddingMode.PKCS7);

        using var ms = new MemoryStream();
        ms.Write(Magic);
        ms.WriteByte(FormatVersion);
        ms.Write(salt);
        ms.Write(iv);
        ms.Write(ciphertext);

        using var hmac = new HMACSHA256(macKey);
        var mac = hmac.ComputeHash(ms.ToArray());
        ms.Write(mac);
        return ms.ToArray();
    }

    public static byte[] Decrypt(byte[] container, string password)
    {
        if (container.Length < HeaderSize + MacSize)
            throw new ExportFormatException("container too short");
        if (!container.AsSpan(0, 4).SequenceEqual(Magic))
            throw new ExportFormatException("bad magic");
        if (container[4] != FormatVersion)
            throw new ExportFormatException($"unknown format version {container[4]}");

        var salt = container.AsSpan(5, SaltSize).ToArray();
        var iv = container.AsSpan(5 + SaltSize, IvSize).ToArray();
        var (aesKey, macKey) = DeriveKeys(password, salt);

        var macOffset = container.Length - MacSize;
        using var hmac = new HMACSHA256(macKey);
        var expected = hmac.ComputeHash(container, 0, macOffset);
        if (!CryptographicOperations.FixedTimeEquals(expected, container.AsSpan(macOffset)))
            throw new ExportPasswordException("HMAC mismatch");

        var ciphertext = container.AsSpan(HeaderSize, macOffset - HeaderSize).ToArray();
        using var aes = Aes.Create();
        aes.Key = aesKey;
        return aes.DecryptCbc(ciphertext, iv, PaddingMode.PKCS7);
    }

    private static (byte[] AesKey, byte[] MacKey) DeriveKeys(string password, byte[] salt)
    {
        var derived = Rfc2898DeriveBytes.Pbkdf2(
            Encoding.UTF8.GetBytes(password), salt, Pbkdf2Iterations, HashAlgorithmName.SHA256, 64);
        return (derived.AsSpan(0, 32).ToArray(), derived.AsSpan(32, 32).ToArray());
    }
}
```

- [ ] **Step 4: テストが通ることを確認**

Run: `dotnet test --filter ExportContainerTests`
Expected: PASS (8 件)。PBKDF2 60 万回はテスト 1 件あたり最大 1 秒程度かかるが許容する。

- [ ] **Step 5: Commit**

```bash
git add src/TNDrop.App/Core/ExportContainer.cs src/TNDrop.Tests/ExportContainerTests.cs
git commit -m "feat: add ExportContainer password crypto for .tndexport (v1.6 Task 2)"
```

---

### Task 3: AppSettings 追加プロパティ

**Files:**
- Modify: `src/TNDrop.App/Core/AppSettings.cs` (末尾、`ShelfPinned` の後)
- Test: `src/TNDrop.Tests/SettingsStoreTests.cs` (既存に追記)

**Interfaces:**
- Consumes: 既存の `AppSettings` / `SettingsStore`
- Produces: `AppSettings.AutoBackupEnabled` (bool, 既定 true)、`AppSettings.LastAutoBackupDate` (string, 既定 "")。Task 6 の App と Task 8 の SettingsWindow が読む。

- [ ] **Step 1: 失敗するテストを書く**

`src/TNDrop.Tests/SettingsStoreTests.cs` の既存クラスにテストを追加 (既存の一時ディレクトリ流儀に合わせる):

```csharp
[Fact]
public void BackupSettings_RoundTrip()
{
    var store = new SettingsStore(_dir);
    store.Save(new AppSettings { AutoBackupEnabled = false, LastAutoBackupDate = "2026-08-29" });

    var s = store.Load();
    Assert.False(s.AutoBackupEnabled);
    Assert.Equal("2026-08-29", s.LastAutoBackupDate);
}

[Fact]
public void BackupSettings_Defaults()
{
    var s = new AppSettings();
    Assert.True(s.AutoBackupEnabled);
    Assert.Equal("", s.LastAutoBackupDate);
}
```

(既存 `SettingsStoreTests` のフィールド名が `_dir` でない場合はそのクラスの流儀に合わせること。)

- [ ] **Step 2: 実行して失敗を確認**

Run: `dotnet test --filter SettingsStoreTests`
Expected: コンパイルエラーで FAIL

- [ ] **Step 3: 実装**

`src/TNDrop.App/Core/AppSettings.cs` の `ShelfPinned` プロパティの後に追加:

```csharp
/// <summary>v1.6: 自動バックアップ (日次 + 終了時) を有効にするか。手動/退避には影響しない。</summary>
public bool AutoBackupEnabled { get; set; } = true;

/// <summary>v1.6: 最後に日次自動バックアップを取った日 ("yyyy-MM-dd"、ローカル日付)。
/// 同日 2 回目の日次バックアップを抑止するための記録で、空文字は「まだ一度もない」。</summary>
public string LastAutoBackupDate { get; set; } = "";
```

- [ ] **Step 4: テストが通ることを確認**

Run: `dotnet test --filter SettingsStoreTests`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add src/TNDrop.App/Core/AppSettings.cs src/TNDrop.Tests/SettingsStoreTests.cs
git commit -m "feat: add auto-backup settings (v1.6 Task 3)"
```

---

### Task 4: ItemStore のデータファイル入出力 API

**Files:**
- Modify: `src/TNDrop.App/Core/ItemStore.cs`
- Test: `src/TNDrop.Tests/ItemStoreBackupApiTests.cs` (新規)

**Interfaces:**
- Consumes: 既存の `_lock` / `_itemsPath` / `BlobsDir` / `JsonOptions` / `TryLoadFrom` / `Load()`
- Produces (Task 5 の BackupService が使う):
  - `public event Action? Saved;` — Save() が成功したときに発火 (日次自動バックアップの契機)
  - `public void CopyDataTo(string destDir)` — ロック下で items.dat (あれば) と blobs\ を destDir へコピー
  - `public string? ReadDecryptedJson()` — ロック下で items.dat を DPAPI 復号した JSON 文字列 (無ければ null)
  - `public static void WriteEncryptedJson(string path, string json)` — JSON を DPAPI 暗号化して path へ書く (インポートのステージング用)
  - `public static bool CanDecrypt(string itemsDatPath)` — この環境の DPAPI で復号できるか (リストア前検証)
  - `public void ReplaceDataFrom(string srcDir)` — ロック下で items.dat と blobs\ を srcDir の内容で置き換えてから内部でリロードし `Changed` を発火。srcDir に items.dat が無い場合は空履歴になる。**srcDir の items.dat が存在するのに読めない (復号/パース失敗) 場合は `InvalidDataException` を投げる** — 起動時 Load() の「読めなければ空で続行」という耐障害性をここで流用すると、壊れたバックアップのリストアが履歴を静かに空にしてしまう。復元には巻き戻し経路があるので、ここは大声で失敗するのが正しい (Task 5 の巻き戻しテストがこの例外を利用する)

- [ ] **Step 1: 失敗するテストを書く**

`src/TNDrop.Tests/ItemStoreBackupApiTests.cs` (既存 `ItemStorePersistenceTests` の一時ディレクトリ流儀を踏襲):

```csharp
using TNDrop.Core;

public class ItemStoreBackupApiTests : IDisposable
{
    private readonly string _dir = Path.Combine(Path.GetTempPath(), "tndrop-test-" + Guid.NewGuid());
    private readonly string _dest = Path.Combine(Path.GetTempPath(), "tndrop-test-" + Guid.NewGuid());

    public void Dispose()
    {
        try { Directory.Delete(_dir, true); } catch { }
        try { Directory.Delete(_dest, true); } catch { }
    }

    private ItemStore NewStoreWithOneItem(string text)
    {
        var store = new ItemStore(_dir);
        store.TryAdd(new ClipItem { Kind = ClipKind.Text, Text = text, CreatedAtUtc = DateTime.UtcNow });
        store.Save();
        return store;
    }

    [Fact]
    public void Saved_RaisedAfterSuccessfulSave()
    {
        var store = new ItemStore(_dir);
        var raised = 0;
        store.Saved += () => raised++;
        store.Save();
        Assert.Equal(1, raised);
    }

    [Fact]
    public void CopyDataTo_CopiesItemsDatAndBlobs()
    {
        var store = NewStoreWithOneItem("hello");
        File.WriteAllBytes(Path.Combine(store.BlobsDir, "img1.png"), new byte[] { 1, 2, 3 });

        store.CopyDataTo(_dest);

        Assert.True(File.Exists(Path.Combine(_dest, "items.dat")));
        Assert.True(File.Exists(Path.Combine(_dest, "blobs", "img1.png")));
    }

    [Fact]
    public void ReadDecryptedJson_ReturnsJsonContainingItemText()
    {
        var store = NewStoreWithOneItem("秘密のテキスト");
        var json = store.ReadDecryptedJson();
        Assert.NotNull(json);
        Assert.Contains("秘密のテキスト", json);
    }

    [Fact]
    public void ReadDecryptedJson_NoFile_ReturnsNull()
    {
        var store = new ItemStore(_dir);
        Assert.Null(store.ReadDecryptedJson());
    }

    [Fact]
    public void WriteEncryptedJson_ProducesFileThatLoadLoads()
    {
        var store = NewStoreWithOneItem("roundtrip");
        var json = store.ReadDecryptedJson()!;

        var otherDir = Path.Combine(_dest, "other");
        Directory.CreateDirectory(otherDir);
        ItemStore.WriteEncryptedJson(Path.Combine(otherDir, "items.dat"), json);

        var other = new ItemStore(otherDir);
        other.Load();
        Assert.Contains(other.Items, i => i.Text == "roundtrip");
    }

    [Fact]
    public void CanDecrypt_TrueForOwnFile_FalseForGarbage()
    {
        var store = NewStoreWithOneItem("x");
        Assert.True(ItemStore.CanDecrypt(Path.Combine(_dir, "items.dat")));

        var garbage = Path.Combine(_dest, "items.dat");
        Directory.CreateDirectory(_dest);
        File.WriteAllBytes(garbage, new byte[] { 9, 9, 9, 9 });
        Assert.False(ItemStore.CanDecrypt(garbage));
    }

    [Fact]
    public void ReplaceDataFrom_CorruptItemsDat_Throws()
    {
        var source = NewStoreWithOneItem("keep");
        Directory.CreateDirectory(_dest);
        File.WriteAllBytes(Path.Combine(_dest, "items.dat"), new byte[] { 1, 2, 3, 4 });

        Assert.Throws<InvalidDataException>(() => source.ReplaceDataFrom(_dest));
    }

    [Fact]
    public void ReplaceDataFrom_SwapsContentAndRaisesChanged()
    {
        var source = NewStoreWithOneItem("old-content");
        var stagingStore = new ItemStore(_dest);
        stagingStore.TryAdd(new ClipItem { Kind = ClipKind.Text, Text = "new-content", CreatedAtUtc = DateTime.UtcNow });
        stagingStore.Save();

        var changed = false;
        source.Changed += () => changed = true;
        source.ReplaceDataFrom(_dest);

        Assert.True(changed);
        Assert.Contains(source.Items, i => i.Text == "new-content");
        Assert.DoesNotContain(source.Items, i => i.Text == "old-content");
    }
}
```

注意: `ItemStore.Items` / `Changed` / `TryAdd` の正確な名前・シグネチャは実装前に `ItemStore.cs` で確認し、テストをそれに合わせること (`Items` 相当の公開列挙と `Changed` 相当のイベントは既存 UI が使っているので必ず存在する。名前が違う場合はテスト側を直す — 本体の改名はしない)。

- [ ] **Step 2: 実行して失敗を確認**

Run: `dotnet test --filter ItemStoreBackupApiTests`
Expected: コンパイルエラーで FAIL

- [ ] **Step 3: 実装**

`src/TNDrop.App/Core/ItemStore.cs` に追加 (Save() / Load() の近く)。ポイント:

- `Saved` イベント: `Save()` の末尾、try が成功した場合のみ、**ロックの外で**発火する (ハンドラが CopyDataTo を呼んでもデッドロックしないように)。既存 Save() の catch でログして続行する構造は変えない。成功フラグをロック内で立て、`lock` ブロックを出た後に `if (ok) Saved?.Invoke();`。
- `CopyDataTo`: `lock (_lock)` 内で `Directory.CreateDirectory(destDir)` → items.dat があれば `File.Copy(_itemsPath, Path.Combine(destDir, "items.dat"), overwrite: true)` → blobs は `Directory.CreateDirectory(destDir\blobs)` して全ファイルを `File.Copy` (サブディレクトリは存在しない前提でフラットにコピー)。
- `ReadDecryptedJson`: `lock (_lock)` 内で `File.Exists(_itemsPath)` チェック → `ProtectedData.Unprotect(File.ReadAllBytes(_itemsPath), null, DataProtectionScope.CurrentUser)` → UTF-8 文字列。存在しなければ null。復号失敗は例外をそのまま投げる (呼び出し元が扱う)。
- `WriteEncryptedJson` (static): `File.WriteAllBytes(path, ProtectedData.Protect(Encoding.UTF8.GetBytes(json), null, DataProtectionScope.CurrentUser))`。
- `CanDecrypt` (static): try で `ProtectedData.Unprotect(File.ReadAllBytes(itemsDatPath), null, DataProtectionScope.CurrentUser)` → true、catch (`CryptographicException` / `IOException` / `FormatException` 等はまとめて) → false。
- `ReplaceDataFrom`: `lock (_lock)` 内で:
  0. 先に検証: `srcDir\items.dat` が存在する場合、その場で復号+デシリアライズを試み、失敗したら**何も差し替えずに** `InvalidDataException` を投げる (Interfaces 節の理由を `<summary>` に書く)。成功したらその結果リストを手順 3 で使う (検証と読み込みを二度やらない — 1 回の解決から両方を導く)
  1. `_items` 相当のリストをクリアせずまず ファイル差し替え: items.dat / items.bak / items.tmp を削除 (存在すれば) → `srcDir\items.dat` があれば `_itemsPath` へコピー
  2. blobs: `BlobsDir` の中身を全削除 → `srcDir\blobs\*` をコピー (srcDir\blobs が無ければ空のまま)
  3. ロック内でメモリを再構築: 手順 0 で読んだリストで `_items` を差し替え (items.dat が無ければ空リスト)
  4. ロックの外で `Changed` を発火 (既存の Changed 発火作法に合わせる — 既存メソッドが Changed をどう発火しているかを 1 つ読んで同じ形にする)
- 追加メソッドすべてに、既存スタイルに合わせた `<summary>` を付ける (この計画の Interfaces 節の役割説明を要約)。

- [ ] **Step 4: テストが通ることを確認**

Run: `dotnet test --filter ItemStoreBackupApiTests`
Expected: PASS (8 件)。既存テストにも影響がないこと: `dotnet test --filter ItemStore` で全 green。

- [ ] **Step 5: Commit**

```bash
git add src/TNDrop.App/Core/ItemStore.cs src/TNDrop.Tests/ItemStoreBackupApiTests.cs
git commit -m "feat: ItemStore backup/restore file APIs (v1.6 Task 4)"
```

---

### Task 5: BackupService (作成・一覧・検証・復元・移行)

**Files:**
- Create: `src/TNDrop.App/Core/BackupService.cs`
- Test: `src/TNDrop.Tests/BackupServiceTests.cs`

**Interfaces:**
- Consumes: Task 1 `BackupPruning` / Task 2 `ExportContainer` / Task 4 の ItemStore API
- Produces (Task 6 の App と Task 7 の BackupDialog が使う):

```csharp
public sealed record BackupEntry(string FilePath, BackupKind Kind, DateTime CreatedLocal);

public enum BackupValidation { Ok, NotABackup, WrongEnvironment }

public sealed class BackupRestoreException : Exception
{
    public bool RolledBack { get; }
    public BackupRestoreException(string message, bool rolledBack, Exception? inner = null);
}

public sealed class BackupService
{
    public BackupService(string dataDir, ItemStore store);
    public string BackupsDir { get; }                       // dataDir\backups
    public string? CreateBackup(BackupKind kind);           // 成功: 作成した ZIP パス / 失敗: null (ログのみ)
    public IReadOnlyList<BackupEntry> ListBackups();        // 新しい順
    public void DeleteBackup(string filePath);
    public BackupValidation Validate(string zipPath);
    public void RestoreFrom(string zipPath);                // 退避→差し替え。失敗時 BackupRestoreException
    public void ExportTo(string destPath, string password); // .tndexport 作成
    public void ImportFrom(string srcPath, string password); // 復号→パス書換→RestoreFrom 相当
}
```

- 復元後の「設定再読込 + UI 再適用 + 完了メッセージ」は Task 6 の App 側の責務 (BackupService はファイルと ItemStore までしか触らない)。

- [ ] **Step 1: 失敗するテストを書く**

`src/TNDrop.Tests/BackupServiceTests.cs`:

```csharp
using System.IO.Compression;
using TNDrop.Core;

public class BackupServiceTests : IDisposable
{
    private readonly string _dir = Path.Combine(Path.GetTempPath(), "tndrop-test-" + Guid.NewGuid());
    private readonly ItemStore _store;
    private readonly BackupService _svc;

    public BackupServiceTests()
    {
        _store = new ItemStore(_dir);
        _svc = new BackupService(_dir, _store);
    }

    public void Dispose()
    {
        try { Directory.Delete(_dir, true); } catch { }
    }

    private void SeedItem(string text)
    {
        _store.TryAdd(new ClipItem { Kind = ClipKind.Text, Text = text, CreatedAtUtc = DateTime.UtcNow });
        _store.Save();
    }

    [Fact]
    public void CreateBackup_Manual_ProducesZipWithManifestItemsAndSettings()
    {
        SeedItem("a");
        File.WriteAllText(Path.Combine(_dir, "settings.json"), "{}");
        File.WriteAllBytes(Path.Combine(_store.BlobsDir, "b.png"), new byte[] { 1 });

        var path = _svc.CreateBackup(BackupKind.Manual);

        Assert.NotNull(path);
        Assert.StartsWith("manual-", Path.GetFileName(path));
        using var zip = ZipFile.OpenRead(path!);
        Assert.NotNull(zip.GetEntry("manifest.json"));
        Assert.NotNull(zip.GetEntry("items.dat"));
        Assert.NotNull(zip.GetEntry("settings.json"));
        Assert.NotNull(zip.GetEntry("blobs/b.png"));
    }

    [Fact]
    public void CreateBackup_Auto_SameDayOverwrites_SingleFile()
    {
        SeedItem("a");
        _svc.CreateBackup(BackupKind.Auto);
        _svc.CreateBackup(BackupKind.Auto);

        var autos = Directory.GetFiles(_svc.BackupsDir, "auto-*.zip");
        Assert.Single(autos);
    }

    [Fact]
    public void ListBackups_NewestFirst_WithKinds()
    {
        SeedItem("a");
        _svc.CreateBackup(BackupKind.Auto);
        _svc.CreateBackup(BackupKind.Manual);

        var list = _svc.ListBackups();
        Assert.Equal(2, list.Count);
        Assert.Contains(list, e => e.Kind == BackupKind.Auto);
        Assert.Contains(list, e => e.Kind == BackupKind.Manual);
    }

    [Fact]
    public void Validate_RandomZip_NotABackup()
    {
        var stray = Path.Combine(_dir, "stray.zip");
        using (var zip = ZipFile.Open(stray, ZipArchiveMode.Create))
            zip.CreateEntry("nothing.txt");
        Assert.Equal(BackupValidation.NotABackup, _svc.Validate(stray));
    }

    [Fact]
    public void RestoreFrom_ReplacesCurrentData_AndWritesSafetyBackup()
    {
        SeedItem("old");
        var backup = _svc.CreateBackup(BackupKind.Manual)!;
        SeedItem("newer-than-backup");

        _svc.RestoreFrom(backup);

        Assert.Contains(_store.Items, i => i.Text == "old");
        Assert.DoesNotContain(_store.Items, i => i.Text == "newer-than-backup");
        Assert.NotEmpty(Directory.GetFiles(_svc.BackupsDir, "safety-*.zip"));
    }

    [Fact]
    public void RestoreFrom_CorruptItemsDat_RollsBackAndThrows()
    {
        SeedItem("survives-rollback");
        // manifest と settings は正しいが items.dat が DPAPI で読めないバックアップを偽造する。
        // Validate は通らないが、RestoreFrom はダイアログの Validate に依存してはならない。
        var good = _svc.CreateBackup(BackupKind.Manual)!;
        var corrupt = Path.Combine(_dir, "corrupt.zip");
        File.Copy(good, corrupt);
        using (var zip = ZipFile.Open(corrupt, ZipArchiveMode.Update))
        {
            zip.GetEntry("items.dat")!.Delete();
            using var w = new StreamWriter(zip.CreateEntry("items.dat").Open());
            w.Write("garbage");
        }

        var ex = Assert.Throws<BackupRestoreException>(() => _svc.RestoreFrom(corrupt));

        Assert.True(ex.RolledBack);
        Assert.Contains(_store.Items, i => i.Text == "survives-rollback");
    }

    [Fact]
    public void ExportImport_RoundTrip_MigratesItems()
    {
        SeedItem("移行するテキスト");
        File.WriteAllText(Path.Combine(_dir, "settings.json"), "{\"Edge\":\"Right\"}");
        var exportPath = Path.Combine(_dir, "out.tndexport");

        _svc.ExportTo(exportPath, "pass-word-8");

        // 「別端末」を新しい dataDir で模擬する (DPAPI は同一ユーザーなので通る)
        var otherDir = Path.Combine(Path.GetTempPath(), "tndrop-test-" + Guid.NewGuid());
        try
        {
            var otherStore = new ItemStore(otherDir);
            var otherSvc = new BackupService(otherDir, otherStore);
            otherSvc.ImportFrom(exportPath, "pass-word-8");

            Assert.Contains(otherStore.Items, i => i.Text == "移行するテキスト");
            Assert.True(File.Exists(Path.Combine(otherDir, "settings.json")));
        }
        finally
        {
            try { Directory.Delete(otherDir, true); } catch { }
        }
    }

    [Fact]
    public void ImportFrom_RewritesBlobPathsUnderOldDataDir()
    {
        var blobFile = Path.Combine(_store.BlobsDir, "conv.png");
        File.WriteAllBytes(blobFile, new byte[] { 1 });
        _store.TryAdd(new ClipItem
        {
            Kind = ClipKind.Files,
            Paths = new List<string> { blobFile, @"C:\somewhere\user-file.txt" },
            CreatedAtUtc = DateTime.UtcNow,
        });
        _store.Save();
        var exportPath = Path.Combine(_dir, "out.tndexport");
        _svc.ExportTo(exportPath, "pass-word-8");

        var otherDir = Path.Combine(Path.GetTempPath(), "tndrop-test-" + Guid.NewGuid());
        try
        {
            var otherStore = new ItemStore(otherDir);
            var otherSvc = new BackupService(otherDir, otherStore);
            otherSvc.ImportFrom(exportPath, "pass-word-8");

            var item = otherStore.Items.Single(i => i.Kind == ClipKind.Files);
            Assert.Contains(Path.Combine(otherStore.BlobsDir, "conv.png"), item.Paths);
            Assert.Contains(@"C:\somewhere\user-file.txt", item.Paths); // blobs 外は書き換えない
            Assert.DoesNotContain(blobFile, item.Paths);
        }
        finally
        {
            try { Directory.Delete(otherDir, true); } catch { }
        }
    }

    [Fact]
    public void ImportFrom_WrongPassword_Throws_AndLeavesDataUntouched()
    {
        SeedItem("keep-me");
        var exportPath = Path.Combine(_dir, "out.tndexport");
        _svc.ExportTo(exportPath, "pass-word-8");

        Assert.Throws<ExportPasswordException>(() => _svc.ImportFrom(exportPath, "wrong-password"));
        Assert.Contains(_store.Items, i => i.Text == "keep-me");
    }

    [Fact]
    public void ExportTo_ShortPassword_Throws()
    {
        SeedItem("a");
        Assert.Throws<ArgumentException>(() => _svc.ExportTo(Path.Combine(_dir, "x.tndexport"), "short"));
    }

    [Fact]
    public void CreateBackup_PrunesOldAutoBackups()
    {
        SeedItem("a");
        // 過去 7 日分の自動バックアップを偽造してから 8 日目を作る
        Directory.CreateDirectory(_svc.BackupsDir);
        for (var d = 1; d <= 7; d++)
        {
            var fake = Path.Combine(_svc.BackupsDir, $"auto-2001010{d}.zip");
            using var zip = ZipFile.Open(fake, ZipArchiveMode.Create);
            zip.CreateEntry("manifest.json");
        }

        _svc.CreateBackup(BackupKind.Auto);

        var autos = Directory.GetFiles(_svc.BackupsDir, "auto-*.zip");
        Assert.Equal(7, autos.Length);
        Assert.DoesNotContain(autos, p => Path.GetFileName(p) == "auto-20010101.zip");
    }
}
```

(Task 4 と同じ注意: `Items` / `TryAdd` の実名に合わせる。)

- [ ] **Step 2: 実行して失敗を確認**

Run: `dotnet test --filter BackupServiceTests`
Expected: コンパイルエラーで FAIL

- [ ] **Step 3: 実装**

`src/TNDrop.App/Core/BackupService.cs`。構造 (設計書 §3〜§6):

```csharp
using System;
using System.Collections.Generic;
using System.IO;
using System.IO.Compression;
using System.Linq;
using System.Text;
using System.Text.Json;
using System.Text.Json.Serialization;
using TNDrop.Services; // FileLogger の名前空間は実物に合わせる

namespace TNDrop.Core;
```

主要な実装ポイント:

1. **manifest**: `private sealed class BackupManifest { public int Format { get; set; } = 1; public string AppVersion { get; set; } = ""; public DateTime CreatedUtc { get; set; } public string Kind { get; set; } = ""; public string DataDir { get; set; } = ""; }`。`AppVersion` は既存の `AppVersion.Display` (TrayIcon.ShowAbout が使っているクラス) から取る。JSON は camelCase (`JsonSerializerOptions { PropertyNamingPolicy = JsonNamingPolicy.CamelCase }`) で読み書きし、manifest 内キーは設計書 §3 のとおり小文字になる。
2. **CreateBackup(kind)**:
   - ファイル名: `Auto` → `auto-{DateTime.Now:yyyyMMdd}.zip` (同名があれば削除してから作成 = 同日上書き)、`Manual`/`Safety` → `{prefix}-{DateTime.Now:yyyyMMdd-HHmmss}.zip`。
   - 手順: `BackupsDir` 作成 → 一時ステージング `dataDir\tmp\backup-{Guid:N}` に `store.CopyDataTo(staging)` + `settings.json` があれば `File.Copy` → manifest.json を書く → `ZipFile.CreateFromDirectory(staging, tmpZip)` → `File.Move(tmpZip, finalPath, overwrite: true)` → ステージング削除 → `Prune()`。
   - 全体を try/catch し、失敗は `FileLogger.Instance?.Warn("backup", ...)` して null を返す (設計書 §4: 自動バックアップの失敗は通常動作を止めない。手動もダイアログ側が null を見て文言を出す)。
   - `Prune()`: `BackupPruning.SelectFilesToDelete(Directory.GetFiles(BackupsDir).Select(Path.GetFileName)!)` の結果を削除 (個別 try/catch、ログのみ)。
3. **ListBackups()**: `Directory.GetFiles(BackupsDir, "*.zip")` を対象に、ファイル名プレフィックスから Kind を判定 (`auto-`/`manual-`/`safety-` 以外は無視)。CreatedLocal は ZIP 内 manifest の `createdUtc` を読んで `ToLocalTime()` (manifest が読めない ZIP は `File.GetLastWriteTime` にフォールバック)。新しい順 (CreatedLocal 降順)。
4. **Validate(zipPath)**: `ZipFile.OpenRead` → manifest.json が読めて format==1 で items.dat エントリと settings.json エントリがある → それ以外は `NotABackup`。次に items.dat エントリを一時ファイルに展開して `ItemStore.CanDecrypt` → false なら `WrongEnvironment` (設計書 §5 手順 3)。ZIP として開けない場合も `NotABackup`。
5. **RestoreFrom(zipPath)** (設計書 §5 手順 2〜6。手順 1 の確認ダイアログは UI 側):
   ```
   var safety = CreateBackup(BackupKind.Safety);
   if (safety is null) throw new BackupRestoreException("safety backup failed", rolledBack: false);
   var staging = ExtractToStaging(zipPath);   // dataDir\tmp\restore-{Guid:N} に展開
   try
   {
       _store.ReplaceDataFrom(staging);
       var stagedSettings = Path.Combine(staging, "settings.json");
       if (File.Exists(stagedSettings))
           File.Copy(stagedSettings, Path.Combine(_dataDir, "settings.json"), overwrite: true);
   }
   catch (Exception ex)
   {
       // 巻き戻し: safety ZIP を展開して同じ経路でもう一度差し替える
       try
       {
           var rollback = ExtractToStaging(safety);
           _store.ReplaceDataFrom(rollback);
           var rbSettings = Path.Combine(rollback, "settings.json");
           if (File.Exists(rbSettings))
               File.Copy(rbSettings, Path.Combine(_dataDir, "settings.json"), overwrite: true);
           throw new BackupRestoreException("restore failed", rolledBack: true, ex);
       }
       catch (BackupRestoreException) { throw; }
       catch (Exception rollbackEx)
       {
           FileLogger.Instance?.Error("backup", "rollback failed", rollbackEx);
           throw new BackupRestoreException("restore failed and rollback failed", rolledBack: false, ex);
       }
   }
   finally { CleanupTmp(); }
   ```
6. **ExportTo(destPath, password)**:
   - `password.Length < ExportContainer.MinPasswordLength` → `ArgumentException`。
   - ステージングに: `store.ReadDecryptedJson()` (null なら `"[]"`) を `items.json` として書く + blobs コピー (`store.CopyDataTo` を使い、できた items.dat は削除して items.json に置き換える) + settings.json + manifest (kind="export"、`DataDir = _dataDir`)。
   - ステージングを ZIP 化 → `File.ReadAllBytes` → `ExportContainer.Encrypt` → destPath へ書く → ステージング/中間 ZIP を finally で削除 (設計書 §6.1 手順 4)。
7. **ImportFrom(srcPath, password)**:
   - `ExportContainer.Decrypt(File.ReadAllBytes(srcPath), password)` (formet/password 例外はそのまま上へ)。
   - 復号 bytes を一時 ZIP に書いて展開 → manifest 検証 (kind=="export"、format==1、items.json あり。不一致は `ExportFormatException`)。
   - **パス書き換え** (設計書 §6.2 手順 4): `JsonSerializer.Deserialize<List<ClipItem>>(itemsJson, ItemStore と同じ JsonOptions)` → 各 `item.Paths` の要素で、`oldBlobs = Path.Combine(manifest.DataDir, "blobs")` 配下 (`Path.GetFullPath` + `OrdinalIgnoreCase` の StartsWith、ItemStore.DeleteBlobPathIfUnderBlobsDir と同じ判定方法) のものを `Path.Combine(_store.BlobsDir, ファイル名)` へ置換 → 再シリアライズ。ItemStore の JsonOptions は `internal static` に昇格して共有する (二重定義しない)。
   - 書き換え後 JSON を `ItemStore.WriteEncryptedJson(staging\items.dat)` で DPAPI 化し、items.json は削除 → 以降は `RestoreFrom` と同じ処理 (safety 作成 → ReplaceDataFrom → settings.json コピー → 巻き戻し) を共通 private メソッド `RestoreFromStaging(string staging)` に括り出して両方から呼ぶ。
8. **tmp 管理**: すべての中間ファイルは `dataDir\tmp\` 配下。各操作の finally で自分が作ったフォルダを削除し、コンストラクタで前回残骸 (`dataDir\tmp` 全体) を try/catch で掃除する。
9. ログ: 各公開操作の開始/成功/失敗を `FileLogger.Instance?.Info/Warn/Error("backup", ...)`。パスワード・履歴本文は出さない (設計書 §8)。

- [ ] **Step 4: テストが通ることを確認**

Run: `dotnet test --filter BackupServiceTests`
Expected: PASS (11 件)

- [ ] **Step 5: 全テスト実行**

Run: `dotnet test`
Expected: 既存含め全 green

- [ ] **Step 6: Commit**

```bash
git add src/TNDrop.App/Core/BackupService.cs src/TNDrop.Tests/BackupServiceTests.cs src/TNDrop.App/Core/ItemStore.cs
git commit -m "feat: BackupService create/list/validate/restore/export/import (v1.6 Task 5)"
```

---

### Task 6: App 配線 (自動契機・SessionEnding・復元オーケストレーション・トレイ項目)

**Files:**
- Modify: `src/TNDrop.App/App.xaml.cs`
- Modify: `src/TNDrop.App/UI/TrayIcon.cs`
- Modify: `src/TNDrop.App/Resources/Strings.cs` / `Strings.resx` / `Strings.en.resx` (TrayBackup のみ。他の文言は Task 7)

**Interfaces:**
- Consumes: Task 5 `BackupService` / Task 3 の設定
- Produces (Task 7 の BackupDialog が使う):
  - `App.Backup` — `public static BackupService? Backup { get; private set; }`
  - `App.SetAutoBackupEnabled(bool)` — 既存 Set* パターン
  - `App.RunRestore(string zipPath)` / `App.RunImport(string srcPath, string password)` — 復元オーケストレーション (監視一時停止 → BackupService → 設定再読込・全 UI 再適用)。成功で true を返す。失敗はここでログ済みの `BackupRestoreException` 等を投げ直し、文言化は Dialog 側
  - `TrayIcon.BackupDialogRequested` イベント + メニュー項目

- [ ] **Step 1: TrayIcon にメニュー項目を追加**

`src/TNDrop.App/UI/TrayIcon.cs`: 既存 `settingsItem` (`TrayIcon.cs:46-47`) と同じパターンで、`settingsItem` の直後に追加し、`menu.Items` では `settingsItem` の次 (`aboutItem` の前) に挿入:

```csharp
public event Action? BackupDialogRequested;
// コンストラクタ内:
var backupItem = new ToolStripMenuItem(Strings.TrayBackup);
backupItem.Click += (_, _) => BackupDialogRequested?.Invoke();
```

文言 3 点セット: `TrayBackup` = ja `データのバックアップ・移行...` / en `Back up and migrate data...`。`Strings.cs` に `public static string TrayBackup => Get(nameof(TrayBackup));`。

- [ ] **Step 2: App.xaml.cs — BackupService 生成と自動契機**

`OnStartup` の `Store = new ItemStore(DataDir); Store.Load();` (App.xaml.cs:163-164) の直後に:

```csharp
Backup = new BackupService(DataDir, Store);
Store.Saved += OnStoreSaved;
SessionEnding += OnSessionEnding;
```

追加メンバー (既存 Set* 群の近くに):

```csharp
public static BackupService? Backup { get; private set; }

/// <summary>日次自動バックアップ (設計書 §4): その日最初の Save 成功後に 1 回。</summary>
private static void OnStoreSaved()
{
    if (!Settings.AutoBackupEnabled)
        return;
    var today = DateTime.Now.ToString("yyyy-MM-dd");
    if (Settings.LastAutoBackupDate == today)
        return;
    if (Backup?.CreateBackup(BackupKind.Auto) is not null)
    {
        Settings.LastAutoBackupDate = today;
        SaveSettings();
    }
}

/// <summary>シャットダウン/サインアウト時 (設計書 §4)。リトライしない — 翌日の日次が拾う。</summary>
private void OnSessionEnding(object sender, SessionEndingCancelEventArgs e)
{
    try
    {
        Store?.Save();
        if (Settings.AutoBackupEnabled)
            Backup?.CreateBackup(BackupKind.Auto);
    }
    catch (Exception ex)
    {
        FileLogger.Instance?.Warn("backup", $"session-ending backup failed: {ex.Message}");
    }
}

public static void SetAutoBackupEnabled(bool value)
{
    Settings.AutoBackupEnabled = value;
    SaveSettings();
}
```

`OnExit` (App.xaml.cs:277-300) の `Store?.Save();` (:292) の直後に (トレイ終了時の「終了時バックアップ」):

```csharp
try
{
    if (Settings.AutoBackupEnabled)
        Backup?.CreateBackup(BackupKind.Auto);
}
catch (Exception ex)
{
    FileLogger.Instance?.Warn("backup", $"exit backup failed: {ex.Message}");
}
```

注意: `OnStoreSaved` は自動バックアップ内の処理で `Store.Save()` を呼ばないので再入しない (CreateBackup はファイルコピーのみ)。`SaveSettings()` は settings.json を書くだけで Store.Saved を発火しない。

- [ ] **Step 3: App.xaml.cs — 復元オーケストレーションと全設定再適用**

```csharp
/// <summary>
/// リストア/インポート後の共通後処理つき実行 (設計書 §5 手順 4〜5)。
/// 成功時 true。BackupService の例外はそのまま上へ (文言化は BackupDialog)。
/// </summary>
public static bool RunRestore(string zipPath)
{
    return RunReplacing(() => Backup!.RestoreFrom(zipPath));
}

public static bool RunImport(string srcPath, string password)
{
    return RunReplacing(() => Backup!.ImportFrom(srcPath, password));
}

private static bool RunReplacing(Action replace)
{
    if (Backup is null || Monitor is null)
        return false;
    var app = (App)Current;
    app._settingsWindow?.Close();          // 開いたままだと差し替え前の値を書き戻すため閉じる
    var pausedBefore = Monitor.Paused;
    Monitor.Paused = true;
    try
    {
        replace();
        ReloadAllSettingsFromDisk();
        return true;
    }
    finally
    {
        Monitor.Paused = Settings.IncognitoMode; // 復元後の設定に従う (旧 pausedBefore ではなく)
    }
}
```

`ReloadAllSettingsFromDisk()` (新規 private static): `Settings` プロパティに `SettingsStore.Load()` の結果を代入し直し、`OnStartup` が設定ロード後に行っている適用処理をすべてやり直す。実装時は `OnStartup` (App.xaml.cs:77-275) の手順 3〜7 と各 `Set*` メソッド (:353-935) を読み、以下のグループを漏れなく再適用すること:

1. UI カルチャ (`ApplyUiCulture` 相当 — `SetLanguage` :673 が呼んでいるものと同じ)
2. `TextScaleMap.Apply(Settings.TextScale)`
3. トレイの表示同期: `_trayIcon.SetHoverEnabled(Settings.HoverEnabled)` / `SetIncognito(Settings.IncognitoMode)`
4. `Monitor.Paused = Settings.IncognitoMode` (RunReplacing の finally と重複してよい)
5. シェルフ/トリガー: `_shelf?.ApplySettings(Settings)` / `_edgeTrigger?.ApplySettings(Settings)` (= `ReapplyPlacement` :800 の中身)
6. インジケーター: `SetIndicatorStyle`/`SetIndicatorColor`/`SetIndicatorOpacityPercent`/`SetIndicatorEnabled` が行っている伝播 (それぞれの Set* 本体から Settings 代入と SaveSettings を除いた適用部分)
7. エッジヒント: `SetEdgeHintEnabled` の適用部分
8. 自動起動レジストリ: `SetAutoStartEnabled` (:935) の適用部分 (復元した設定の AutoStartEnabled を HKCU Run に反映)
9. サウンド設定など、Set* を持つが適用が「Settings を読むだけ」のもの (SoundsEnabled / AutoDelete / MoveToTopOnCopy / PasteOnClick / HistoryCapacity / PurgeUnpinnedOnRestart / PinnedExpanded / ShelfPinned) は再適用不要 — 利用箇所が毎回 `App.Settings` を読むことを 1 つずつ確認し、読んでいないものがあればそこだけ再適用を足す

重複を避けるため、可能なら各 `Set*` の「適用部分」を private ヘルパ (例: `ApplyIndicatorSettings()`) に括り出して `Set*` と `ReloadAllSettingsFromDisk` の両方から呼ぶ形にリファクタしてよい (挙動を変えないこと。既存テストが緑のままであること)。

- [ ] **Step 4: トレイイベントの配線**

`OnStartup` の `_trayIcon.ExitRequested += OnExitRequested;` (App.xaml.cs:192) の隣に:

```csharp
_trayIcon.BackupDialogRequested += OnBackupDialogRequested;
```

`OnOpenSettingsRequested` (:536-554) と同じ単一インスタンス管理で `BackupDialog` を開く `OnBackupDialogRequested()` を追加する (`_backupDialog` フィールド、`Closed` で null 化、既に開いていれば `Activate()`)。BackupDialog クラスは Task 7 で作るため、**このタスクの時点ではコンパイルを通すために `OnBackupDialogRequested` の中身を `FileLogger.Instance?.Info("backup", "dialog requested")` だけにしておき、Task 7 で差し替える** (ビルドを壊さないための順序措置)。

- [ ] **Step 5: ビルドと全テスト**

Run: `dotnet test`
Expected: 全 green (この時点で UI はまだ無いが、既存動作が壊れていないこと)

- [ ] **Step 6: Commit**

```bash
git add src/TNDrop.App/App.xaml.cs src/TNDrop.App/UI/TrayIcon.cs src/TNDrop.App/Resources/Strings.cs src/TNDrop.App/Resources/Strings.resx src/TNDrop.App/Resources/Strings.en.resx
git commit -m "feat: wire auto backup triggers, restore orchestration, tray entry (v1.6 Task 6)"
```

---

### Task 7: BackupDialog + PasswordDialog (UI)

**Files:**
- Create: `src/TNDrop.App/UI/BackupDialog.xaml` / `BackupDialog.xaml.cs`
- Create: `src/TNDrop.App/UI/PasswordDialog.xaml` / `PasswordDialog.xaml.cs`
- Modify: `src/TNDrop.App/App.xaml.cs` (Task 6 で仮置きした `OnBackupDialogRequested` を実装に差し替え)
- Modify: `src/TNDrop.App/Resources/Strings.cs` / `Strings.resx` / `Strings.en.resx`

**Interfaces:**
- Consumes: `App.Backup` / `App.RunRestore` / `App.RunImport` / `BackupService.ListBackups/CreateBackup/DeleteBackup/Validate/ExportTo` / `ExportContainer.MinPasswordLength` / 例外型
- Produces: `public sealed partial class BackupDialog : Window` (引数なしコンストラクタ)、`public sealed partial class PasswordDialog : Window` (`PasswordDialog.ForExport()` / `PasswordDialog.ForImport()` static ファクトリ、`ShowDialog()==true` のとき `Password` プロパティ)

- [ ] **Step 1: 文言を追加 (3 点セット)**

| キー | ja | en |
|---|---|---|
| `BackupDialogTitle` | データのバックアップ・移行 | Back up and migrate data |
| `BackupListLabel` | バックアップ一覧 (履歴・ピン・設定を含む) | Backups (history, pins and settings) |
| `BackupManualButton` | 手動バックアップ(_C) | Back up now(_C) |
| `BackupRestoreButton` | リストア(_R) | Restore(_R) |
| `BackupDeleteButton` | 削除(_D) | Delete(_D) |
| `BackupKindAuto` | [自動] | [Auto] |
| `BackupKindManual` | [手動] | [Manual] |
| `BackupKindSafety` | [退避] | [Safety] |
| `BackupAutoHintOn` | 自動バックアップ: 有効 (日次 + 終了時、7 世代保持) — 変更は設定画面から | Auto backup: on (daily + on exit, keeps 7) - change in Settings |
| `BackupAutoHintOff` | 自動バックアップ: 無効 — 変更は設定画面から | Auto backup: off - change in Settings |
| `BackupMigrationLabel` | 別の PC への移行 (パスワード付き暗号化ファイル) | Migrate to another PC (password-protected file) |
| `BackupExportButton` | エクスポート(_E)... | Export(_E)... |
| `BackupImportButton` | インポート(_I)... | Import(_I)... |
| `BackupRestoreConfirm` | 現在の履歴・ピン・設定は選択したバックアップの内容で置き換えられます。実行前に現在の状態を自動退避します。よろしいですか? | Current history, pins and settings will be replaced by the selected backup. The current state is saved as a safety backup first. Continue? |
| `BackupDeleteConfirm` | 選択したバックアップを削除します。よろしいですか? | Delete the selected backup? |
| `BackupManualFailed` | バックアップの作成に失敗しました。詳細はログを確認してください。 | Backup failed. See the log for details. |
| `BackupRestoreDone` | 復元しました。 | Restored. |
| `BackupRestoreFailedRolledBack` | リストアに失敗しました。元の状態に戻しました。詳細はログを確認してください。 | Restore failed. Your previous data was recovered. See the log for details. |
| `BackupRestoreFailedFatalFormat` | リストアに失敗し、巻き戻しにも失敗しました。退避ファイル: {0}。詳細はログを確認してください。 | Restore failed and rollback also failed. Safety file: {0}. See the log for details. |
| `BackupWrongEnvironment` | このバックアップは別の PC または別のユーザーで作成されたため復元できません。別の PC への移行にはエクスポート機能を使ってください。 | This backup was made on another PC or user account and cannot be restored here. Use Export to migrate between PCs. |
| `BackupNotABackup` | TNDrop のバックアップファイルではありません。 | Not a TNDrop backup file. |
| `ExportDoneFormat` | エクスポートしました: {0} | Exported: {0} |
| `ExportFailed` | エクスポートに失敗しました。詳細はログを確認してください。 | Export failed. See the log for details. |
| `ImportWrongPassword` | パスワードが違うか、ファイルが壊れています。 | Wrong password, or the file is corrupted. |
| `ImportNotExportFile` | TNDrop のエクスポートファイルではありません。 | Not a TNDrop export file. |
| `ExportPasswordTitle` | エクスポートのパスワード設定 | Set export password |
| `ImportPasswordTitle` | パスワードの入力 | Enter password |
| `PasswordLabel` | パスワード (8 文字以上) | Password (8+ characters) |
| `PasswordConfirmLabel` | パスワード (確認) | Password (confirm) |
| `PasswordHint` | 覚えやすい言葉の組み合わせで 8 文字以上にしてください。 | Use a memorable combination of words, 8 characters or longer. |
| `PasswordTooShort` | パスワードは 8 文字以上にしてください。 | Password must be at least 8 characters. |
| `PasswordMismatch` | 確認用のパスワードが一致しません。 | Passwords do not match. |
| `PasswordOk` | OK | OK |
| `PasswordCancel` | キャンセル | Cancel |
| `ExportFileFilter` | TNDrop エクスポート (*.tndexport)\|*.tndexport | TNDrop export (*.tndexport)\|*.tndexport |

(resx の `<data name="..." xml:space="preserve"><value>...</value></data>` 形式は `Strings.resx:209-211` の既存例に合わせる。`{0}` を含むものは `string.Format` で使う。)

- [ ] **Step 2: PasswordDialog を作る**

`PasswordDialog.xaml`: `SettingsWindow.xaml` と同じ標準ウインドウクロム (`WindowStartupLocation="CenterScreen"`, `ShowInTaskbar="False"`, `ResizeMode="NoResize"`, 幅 380)。縦に: ラベル + `PasswordBox x:Name="Box1"` / (エクスポート時のみ表示) 確認ラベル + `PasswordBox x:Name="Box2"` / ヒント `TextBlock` / エラー `TextBlock x:Name="ErrorText"` (赤系、初期 Collapsed) / OK・キャンセルボタン行。

`PasswordDialog.xaml.cs`:

```csharp
public sealed partial class PasswordDialog : Window
{
    private readonly bool _confirmMode;
    public string Password { get; private set; } = "";

    private PasswordDialog(bool confirmMode) { InitializeComponent(); _confirmMode = confirmMode; /* Title と表示切替、文言配線 */ }

    public static PasswordDialog ForExport() => new(confirmMode: true) { Title = Strings.ExportPasswordTitle };
    public static PasswordDialog ForImport() => new(confirmMode: false) { Title = Strings.ImportPasswordTitle };

    private void OnOkClick(object sender, RoutedEventArgs e)
    {
        if (Box1.Password.Length < ExportContainer.MinPasswordLength)
        { ShowError(Strings.PasswordTooShort); return; }
        if (_confirmMode && Box1.Password != Box2.Password)
        { ShowError(Strings.PasswordMismatch); return; }
        Password = Box1.Password;
        DialogResult = true;
    }
}
```

インポート時 (`confirmMode==false`) は確認欄とヒントを Collapsed にし、長さ検証もしない (古いファイルを開けなくしないため — 検証はエクスポート側の責務)。

- [ ] **Step 3: BackupDialog を作る**

`BackupDialog.xaml`: 承認済みモックアップの構成。標準ウインドウクロム (SettingsWindow と同様)、`Width="480"`, `SizeToContent="Height"`, `ResizeMode="NoResize"`:

- `TextBlock` (BackupListLabel) / `ListBox x:Name="BackupList" Height="160"` と右側 `StackPanel` に 3 ボタン (`ManualButton` / `RestoreButton` / `DeleteButton`、`MinWidth="150"`) を `Grid` 2 列で
- ヒント行 `TextBlock x:Name="AutoHintText"` (小さめ・薄め)
- `Separator`
- `TextBlock` (BackupMigrationLabel) / 横並び `ExportButton` `ImportButton`

`BackupDialog.xaml.cs` の要点:

```csharp
public sealed partial class BackupDialog : Window
{
    public BackupDialog()
    {
        InitializeComponent();
        Title = Strings.BackupDialogTitle;
        // 各ボタン Content / ラベル Text を Strings から配線 (SettingsWindow と同じ流儀)
        RefreshList();
        BackupList.SelectionChanged += (_, _) => UpdateButtonStates();
    }

    private void RefreshList()
    {
        var entries = global::TNDrop.App.Backup?.ListBackups() ?? Array.Empty<BackupEntry>();
        BackupList.ItemsSource = entries
            .Select(e => new { Entry = e, Display = $"{KindLabel(e.Kind)} {e.CreatedLocal:yyyy/MM/dd HH:mm:ss}" })
            .ToList();
        BackupList.DisplayMemberPath = "Display";
        AutoHintText.Text = global::TNDrop.App.Settings.AutoBackupEnabled
            ? Strings.BackupAutoHintOn : Strings.BackupAutoHintOff;
        UpdateButtonStates();   // リストア/削除は未選択なら IsEnabled=false
    }

    private static string KindLabel(BackupKind k) => k switch
    {
        BackupKind.Auto => Strings.BackupKindAuto,
        BackupKind.Manual => Strings.BackupKindManual,
        _ => Strings.BackupKindSafety,
    };
}
```

ハンドラ:

- 手動バックアップ: `App.Backup?.CreateBackup(BackupKind.Manual)` → null なら `MessageBox.Show(Strings.BackupManualFailed, ...)` → `RefreshList()`。
- リストア: 選択エントリに対し `Validate` → `WrongEnvironment` なら `BackupWrongEnvironment`、`NotABackup` なら `BackupNotABackup` を表示して中断 → `MessageBox.Show(BackupRestoreConfirm, OKCancel)` → OK なら try `App.RunRestore(path)` → 成功 `BackupRestoreDone` / catch `BackupRestoreException ex` → `ex.RolledBack ? BackupRestoreFailedRolledBack : string.Format(BackupRestoreFailedFatalFormat, safetyの場所)` (safety の場所はダイアログでは特定できないため `BackupsDir` を渡す) → `RefreshList()`。
- 削除: 確認 → `App.Backup?.DeleteBackup(path)` → `RefreshList()`。
- エクスポート: `PasswordDialog.ForExport()` → OK なら `SaveFileDialog { Filter = Strings.ExportFileFilter, FileName = $"TNDrop-export-{DateTime.Now:yyyyMMdd}.tndexport" }` → try `App.Backup?.ExportTo(path, pw)` → `string.Format(ExportDoneFormat, path)` / catch → `ExportFailed` + ログ。
- インポート: `OpenFileDialog { Filter = Strings.ExportFileFilter }` → `PasswordDialog.ForImport()` → リストアと同じ確認文 → try `App.RunImport(path, pw)` → `BackupRestoreDone` / catch `ExportPasswordException` → `ImportWrongPassword` / `ExportFormatException` → `ImportNotExportFile` / `BackupRestoreException` → リストアと同じ出し分け → `RefreshList()`。

すべての MessageBox は `MessageBox.Show(this, text, Strings.BackupDialogTitle, ...)` でこのウインドウをオーナーにする。

- [ ] **Step 4: App の仮置きハンドラを差し替え**

Task 6 Step 4 の `OnBackupDialogRequested` を、`OnOpenSettingsRequested` (App.xaml.cs:536-554) と同じ単一インスタンスパターンで `BackupDialog` を開く実装にする (`private BackupDialog? _backupDialog;` フィールド追加)。

- [ ] **Step 5: ビルド・全テスト・手動スモーク**

Run: `dotnet test`
Expected: 全 green

Run: `dotnet run --project src/TNDrop.App` (または `dotnet build` 後に exe 起動)
確認: トレイ右クリック → 「データのバックアップ・移行...」→ ダイアログが開く / 手動バックアップで一覧に `[手動]` 行が増える / エクスポート → インポート (同一機) が通る / 7 文字パスワードが弾かれる。確認後アプリを終了する。

- [ ] **Step 6: Commit**

```bash
git add src/TNDrop.App/UI/BackupDialog.xaml src/TNDrop.App/UI/BackupDialog.xaml.cs src/TNDrop.App/UI/PasswordDialog.xaml src/TNDrop.App/UI/PasswordDialog.xaml.cs src/TNDrop.App/App.xaml.cs src/TNDrop.App/Resources/Strings.cs src/TNDrop.App/Resources/Strings.resx src/TNDrop.App/Resources/Strings.en.resx
git commit -m "feat: backup/migration dialog and password dialog (v1.6 Task 7)"
```

---

### Task 8: SettingsWindow (トグル・入口ボタン・スライダー下限 100ms)

**Files:**
- Modify: `src/TNDrop.App/UI/SettingsWindow.xaml` / `SettingsWindow.xaml.cs`
- Modify: `src/TNDrop.App/Resources/Strings.cs` / `Strings.resx` / `Strings.en.resx`

**Interfaces:**
- Consumes: `App.SetAutoBackupEnabled` (Task 6) / `App.Backup` / BackupDialog (Task 7) / 既存 `WireCheckBox` (SettingsWindow.xaml.cs:264-281)
- Produces: なし (末端 UI)

- [ ] **Step 1: スライダー下限を 100 に変更**

`SettingsWindow.xaml.cs:235` の `RetractDelaySlider.Minimum = 300;` → `= 100;`、`:239` の `Math.Clamp(settings.RetractDelayMs, 300, 2000)` → `Math.Clamp(settings.RetractDelayMs, 100, 2000)`。(エンジン側 `ShelfWindow.ApplySettings` の `Math.Clamp(s.RetractDelayMs, 100, 10_000)` は既に 100 下限なので変更しない。)

- [ ] **Step 2: 文言追加 (3 点セット)**

| キー | ja | en |
|---|---|---|
| `SettingsAutoBackup` | 自動バックアップ (日次 + 終了時、7 世代保持) | Auto backup (daily + on exit, keeps 7) |
| `SettingsOpenBackupDialog` | データのバックアップ・移行... | Back up and migrate data... |

- [ ] **Step 3: XAML とコード配線**

XAML: 「動作」系 CheckBox 群 (`SettingsWindow.xaml:535-558` 付近) の並びに `<CheckBox x:Name="AutoBackupCheckBox" Margin="0,0,0,2" />` を追加し、その下に `<Button x:Name="OpenBackupDialogButton" HorizontalAlignment="Left" Margin="0,8,0,0" />` を追加 (既存ボタンのスタイルがあればそれに合わせる)。

コード (`SettingsWindow.xaml.cs` の既存配線部):

```csharp
AutoBackupCheckBox.Content = Strings.SettingsAutoBackup;
WireCheckBox(AutoBackupCheckBox, settings.AutoBackupEnabled, global::TNDrop.App.SetAutoBackupEnabled);
OpenBackupDialogButton.Content = Strings.SettingsOpenBackupDialog;
OpenBackupDialogButton.Click += (_, _) => global::TNDrop.App.OpenBackupDialog();
```

`App.OpenBackupDialog()` は Task 6/7 の `OnBackupDialogRequested` を public static に昇格するか、public static ラッパー `public static void OpenBackupDialog() => ((App)Current).OnBackupDialogRequested();` を追加して両方の入口 (トレイ / 設定) から同じ単一インスタンス管理を通す。

- [ ] **Step 4: ビルド・全テスト・手動スモーク**

Run: `dotnet test`
Expected: 全 green

手動: 設定画面でスライダーが 100ms まで下がる / トグル OFF→ON が settings.json に反映される / ボタンからダイアログが開く。

- [ ] **Step 5: Commit**

```bash
git add src/TNDrop.App/UI/SettingsWindow.xaml src/TNDrop.App/UI/SettingsWindow.xaml.cs src/TNDrop.App/Resources/Strings.cs src/TNDrop.App/Resources/Strings.resx src/TNDrop.App/Resources/Strings.en.resx
git commit -m "feat: settings toggle, dialog entry, 100ms retract minimum (v1.6 Task 8)"
```

---

### Task 9: ドキュメント・バージョン・最終検証

**Files:**
- Modify: `docs/manual-test-checklist.md` (§31 追加)
- Modify: `assets/README.html` (v1.6 機能説明 + バージョン表記 3 箇所: `:7` title / `:935` nav / `:1566` foot)
- Modify: `src/TNDrop.App/TNDrop.App.csproj:13` (`<Version>1.6.0</Version>`)
- Modify: `installer/setup.iss:6` (`#define MyAppVersion "1.6.0"`)
- Modify: `CLAUDE.md` (§9 の実装計画ポインタに本計画を追記)

**Interfaces:** なし (ドキュメントのみ)

- [ ] **Step 1: manual-test-checklist.md に §31 を追加**

`docs/manual-test-checklist.md` の §30 の後、実施記録テーブルの前に、§30 と同じ表形式 (`docs/manual-test-checklist.md:354-363` 参照) で追加:

```markdown
## 31. v1.6 追加機能 (バックアップ・リストア・別端末移行)

| # | 操作 | 期待結果 | 結果 |
|---|------|----------|------|
| 31-1 | トレイメニュー「データのバックアップ・移行...」 | ダイアログが開き、一覧が新しい順に表示される | |
| 31-2 | 手動バックアップ | 一覧に [手動] 行が増え、%APPDATA%\TNDrop\backups\ に manual-*.zip ができる | |
| 31-3 | 履歴を変更してからリストア | 確認 → 復元後、履歴・ピン・設定がバックアップ時点に戻り、再起動なしでシェルフに反映される。[退避] 行が増えている | |
| 31-4 | [退避] をリストア | 31-3 の直前の状態に戻る (やり直しが効く) | |
| 31-5 | 削除 | 確認後に一覧と backups\ から消える | |
| 31-6 | [必須] PC をシャットダウン → 再起動 | backups\ の auto-当日.zip が更新されている | |
| 31-7 | トレイ「TNDrop を終了」 | auto-当日.zip が更新される | |
| 31-8 | 設定で自動バックアップ OFF → コピー操作 → 終了 | auto-*.zip が増えない・更新されない。手動は作れる | |
| 31-9 | エクスポート (8 文字未満) | 「8 文字以上」エラーで進めない | |
| 31-10 | エクスポート → 同一機でインポート | パスワード入力 → 確認 → 復元される (画像カード含む) | |
| 31-11 | インポートで誤パスワード | 「パスワードが違うか、ファイルが壊れています。」既存データは無傷 | |
| 31-12 | [必須] 別の Windows ユーザー (または別 PC) で: 旧環境の backups\ の ZIP をリストア | 「別の PC またはユーザーで作成された」案内が出て復元されない | |
| 31-13 | [必須] 別の Windows ユーザー (または別 PC) で: .tndexport をインポート | 履歴・ピン・設定・画像が移行される。画像→ファイルカード変換由来のファイルカードも開ける | |
| 31-14 | 設定の格納遅延スライダー | 100ms まで下げられ、シェルフが約 0.3 秒で格納される | |

---
```

- [ ] **Step 2: README.html 更新**

`assets/README.html` に v1.6 の節を追記 (v1.5 の節の体裁に合わせて: バックアップ・リストアの使い方、自動バックアップの説明、別 PC 移行の手順とパスワードの注意)。バージョン表記 3 箇所を v1.6.0 に更新。

- [ ] **Step 3: バージョン更新**

`src/TNDrop.App/TNDrop.App.csproj:13` → `<Version>1.6.0</Version>` / `installer/setup.iss:6` → `#define MyAppVersion "1.6.0"`。(setup.iss の他の箇所は `{#MyAppVersion}` 参照なので変更不要。`[Files]` への追加も不要 — 新規ファイルはすべて exe に含まれる。)

- [ ] **Step 4: CLAUDE.md の計画ポインタ追記**

`CLAUDE.md` §9 の「実装計画」列挙の末尾に `docs/superpowers/plans/2026-08-29-tndrop-v1.6-backup-migration.md (v1.6)` を追記。

- [ ] **Step 5: 最終検証**

Run: `dotnet test`
Expected: 全 green (既存 439 件 + 本計画で追加した約 30 件)

Run: `powershell -File build.ps1` (test → publish → ISCC が通ること。Inno のビルドは inno-setup-packaging スキルの確定方針に従う — AppId 変更禁止)

- [ ] **Step 6: Commit**

```bash
git add docs/manual-test-checklist.md assets/README.html src/TNDrop.App/TNDrop.App.csproj installer/setup.iss CLAUDE.md
git commit -m "docs: v1.6 checklist section 31, README, version bump to 1.6.0"
```

---

## 実装順序と依存

```
Task 1 (BackupPruning) ─┐
Task 2 (ExportContainer) ┼→ Task 5 (BackupService) → Task 6 (App 配線) → Task 7 (Dialog UI) → Task 8 (SettingsWindow) → Task 9 (docs/version)
Task 3 (AppSettings) ────┤                              ↑ Task 3 も使う
Task 4 (ItemStore API) ──┘
```

Task 1〜4 は互いに独立で並列実装可。Task 5 以降は直列。

## 実機テストゲート (リリース前・利用者実施)

§31 の [必須] 項目 (31-6 シャットダウン、31-12/31-13 別ユーザー/別 PC) は開発機の自動テストでは代替できない。リリース判断の前に docs/manual-test-checklist.md §31 を実機で消化すること。
