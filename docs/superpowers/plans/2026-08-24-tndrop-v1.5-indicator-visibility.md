# TNDrop v1.5 インジケーター視認性改善 実装計画

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 明るい背景でも見えるインジケーター (縁取り + 新スタイル「ふくらみ」) と、色/透明度の設定、自動開始のデフォルト有効化 (一回限り移行) を実装する。

**Architecture:** 色は純粋クラス `IndicatorPalette` (Core) が基準色 1 つから塗り/縁/リムを一括導出し、`IndicatorWindow` は戻り値だけを使う。既存 4 スタイルは「本体+縁」コンテナ化し、第 5 スタイル Bulge をスケール変形アニメーションで追加。設定は既存の AppSettings → SettingsStore サニタイズ → App.Set* 即時反映パターンに従う。自動開始は純粋関数 `SettingsMigration` で一回限り移行。

**Tech Stack:** .NET 10 WPF (self-contained win-x64), xunit + Xunit.StaFact, System.Text.Json。**新規 NuGet 依存の追加は禁止。**

**正となる設計書:** `docs/superpowers/specs/2026-08-24-tndrop-v1.5-indicator-visibility-design.md`

## Global Constraints

- 本番機はオフライン。CDN / 外部フォント / 実行時ダウンロードを書かない。
- XAML / csproj など XML 系コメントの本文に "--" を書かない (単一の "-" を使う。~/.claude/rules/xaml-comments-no-double-dash.md)。
- ソースは UTF-8 (BOM なし) LF。resx は既存ファイルのエンコーディング/形式をそのまま踏襲。
- UI 文言はすべて Strings.resx (ja) と Strings.en.resx (en) の両方に追加し、Strings.cs にプロパティを足す。ハードコード文字列禁止。
- コメント密度・doc comment の書き方は各ファイルの既存スタイルに合わせる。
- テスト実行: `dotnet test TNDrop.sln` (リポジトリルートで実行)。全タスクで全テスト green を確認してからコミット。
- コミットは各タスク末尾の指定メッセージで行う。`git push` はしない。
- インストーラー (installer/setup.iss) は本計画では触らない。

---

### Task A: IndicatorPalette (色の一括導出) + テスト

**Files:**
- Create: `src/TNDrop.App/Core/IndicatorPalette.cs`
- Test: `src/TNDrop.Tests/IndicatorPaletteTests.cs` (新規)

**Interfaces:**
- Produces:
  - `TNDrop.Core.IndicatorPalette.Rgb` — `readonly record struct Rgb(byte R, byte G, byte B)`
  - `IndicatorPalette.Resolve(byte r, byte g, byte b)` → `(Rgb Fill, Rgb Outline, Rgb Rim)`
  - `IndicatorPalette.TryParseHex(string? hex, out Rgb color)` → `bool` (`#RRGGBB` のみ受理)
  - `IndicatorPalette.DefaultColorHex` — `"#5AC8FA"`
  - `IndicatorPalette.Luminance(Rgb c)` → `double` (BT.601)

- [ ] **Step 1: 失敗するテストを書く**

`src/TNDrop.Tests/IndicatorPaletteTests.cs` を新規作成:

```csharp
using TNDrop.Core;

public class IndicatorPaletteTests
{
    // 設計書パート1: 3 色は 1 回の Resolve から導出され、縁は塗りより暗く、
    // リムは塗りより明るい (BT.601 輝度で比較)。
    [Fact]
    public void Resolve_outline_is_darker_and_rim_is_lighter_than_fill()
    {
        var (fill, outline, rim) = IndicatorPalette.Resolve(0x5A, 0xC8, 0xFA);
        Assert.True(IndicatorPalette.Luminance(outline) < IndicatorPalette.Luminance(fill));
        Assert.True(IndicatorPalette.Luminance(rim) > IndicatorPalette.Luminance(fill));
    }

    [Fact]
    public void Resolve_fill_is_the_base_color_unchanged()
    {
        var (fill, _, _) = IndicatorPalette.Resolve(0x0A, 0x84, 0xFF);
        Assert.Equal(new IndicatorPalette.Rgb(0x0A, 0x84, 0xFF), fill);
    }

    // 端の色でも破綻しない: 黒はこれ以上暗くできないので outline == fill (輝度 0)、
    // 白はこれ以上明るくできないので rim == fill (輝度 255)。<= / >= で許容する。
    [Fact]
    public void Resolve_black_and_white_do_not_overflow()
    {
        var black = IndicatorPalette.Resolve(0, 0, 0);
        Assert.True(IndicatorPalette.Luminance(black.Outline) <= IndicatorPalette.Luminance(black.Fill));
        Assert.True(IndicatorPalette.Luminance(black.Rim) > 0);

        var white = IndicatorPalette.Resolve(255, 255, 255);
        Assert.True(IndicatorPalette.Luminance(white.Outline) < 255);
        Assert.True(IndicatorPalette.Luminance(white.Rim) <= IndicatorPalette.Luminance(white.Fill));
    }

    [Theory]
    [InlineData("#5AC8FA", true, 0x5A, 0xC8, 0xFA)]
    [InlineData("#0a84ff", true, 0x0A, 0x84, 0xFF)] // 小文字も受理
    [InlineData("5AC8FA", false, 0, 0, 0)]          // 先頭 # なしは拒否
    [InlineData("#5AC8FA00", false, 0, 0, 0)]       // alpha 付き 8 桁は拒否 (透明度設定と二重になるため)
    [InlineData("#12", false, 0, 0, 0)]
    [InlineData("xyz", false, 0, 0, 0)]
    [InlineData("", false, 0, 0, 0)]
    [InlineData(null, false, 0, 0, 0)]
    public void TryParseHex_accepts_only_rrggbb(string? hex, bool ok, byte r, byte g, byte b)
    {
        Assert.Equal(ok, IndicatorPalette.TryParseHex(hex, out var c));
        if (ok)
        {
            Assert.Equal(new IndicatorPalette.Rgb(r, g, b), c);
        }
    }

    [Fact]
    public void DefaultColorHex_parses()
    {
        Assert.True(IndicatorPalette.TryParseHex(IndicatorPalette.DefaultColorHex, out _));
    }
}
```

- [ ] **Step 2: テストが失敗することを確認**

Run: `dotnet test TNDrop.sln --filter IndicatorPaletteTests`
Expected: コンパイルエラー (IndicatorPalette 未定義) で FAIL。

- [ ] **Step 3: 実装を書く**

`src/TNDrop.App/Core/IndicatorPalette.cs` を新規作成:

```csharp
using System;
using System.Globalization;

namespace TNDrop.Core;

/// <summary>
/// インジケーターの色を基準色 1 つから一括導出する純粋クラス (v1.5)。塗り (Fill)、
/// 縁取り / Bulge 本体 (Outline)、Bulge 外周リム (Rim) の 3 色は必ずこの Resolve
/// 1 回から得る -- 呼び出し側が独自に色を計算して塗りと縁が静かに矛盾する事故を
/// 防ぐ (one-resolution-per-related-fields)。v1.3 の IndicatorBrightness (白方向
/// ブースト) はこのクラスで置き換えられ削除された: 白方向一辺倒の明るさ補正は
/// 明るい背景でちょうど逆効果だった、という本番フィードバックが v1.5 の出発点。
/// 純粋・static なので WPF なしで直接テストできる (IndicatorPaletteTests)。
/// </summary>
public static class IndicatorPalette
{
    /// <summary>設定 IndicatorColor のデフォルト兼パース不能時のフォールバック。</summary>
    public const string DefaultColorHex = "#5AC8FA";

    /// <summary>Outline = 基準色を黒方向へこの率だけブレンド。実機確認で微調整可だが、
    /// 変えるときはこの定数だけを変える (導出式には触れない)。</summary>
    public const double OutlineBlackBlend = 0.65;

    /// <summary>Rim = 基準色を白方向へこの率だけブレンド。</summary>
    public const double RimWhiteBlend = 0.60;

    public readonly record struct Rgb(byte R, byte G, byte B);

    /// <summary>基準色から (塗り, 縁, リム) を一括導出する。塗りは基準色そのまま。</summary>
    public static (Rgb Fill, Rgb Outline, Rgb Rim) Resolve(byte r, byte g, byte b)
    {
        var fill = new Rgb(r, g, b);
        return (fill, Blend(fill, 0, OutlineBlackBlend), Blend(fill, 255, RimWhiteBlend));
    }

    /// <summary>"#RRGGBB" (大文字小文字不問) のみ受理。alpha 付き 8 桁は透明度設定と
    /// 二重になるため受けない (設計書パート2)。</summary>
    public static bool TryParseHex(string? hex, out Rgb color)
    {
        color = default;
        if (hex is null || hex.Length != 7 || hex[0] != '#')
        {
            return false;
        }

        if (!byte.TryParse(hex.AsSpan(1, 2), NumberStyles.HexNumber, CultureInfo.InvariantCulture, out var r) ||
            !byte.TryParse(hex.AsSpan(3, 2), NumberStyles.HexNumber, CultureInfo.InvariantCulture, out var g) ||
            !byte.TryParse(hex.AsSpan(5, 2), NumberStyles.HexNumber, CultureInfo.InvariantCulture, out var b))
        {
            return false;
        }

        color = new Rgb(r, g, b);
        return true;
    }

    /// <summary>ITU-R BT.601 の知覚輝度 (旧 IndicatorBrightness.Luminance と同じ重み)。
    /// テストが「縁は暗い / リムは明るい」を数値で主張するために公開している。</summary>
    public static double Luminance(Rgb c) =>
        (0.299 * c.R) + (0.587 * c.G) + (0.114 * c.B);

    private static Rgb Blend(Rgb from, byte target, double t)
    {
        byte Ch(byte c) => (byte)Math.Round(c + ((target - c) * t), MidpointRounding.AwayFromZero);
        return new Rgb(Ch(from.R), Ch(from.G), Ch(from.B));
    }
}
```

- [ ] **Step 4: テストが通ることを確認**

Run: `dotnet test TNDrop.sln --filter IndicatorPaletteTests`
Expected: 全 PASS。続けて `dotnet test TNDrop.sln` で全体も green。

- [ ] **Step 5: コミット**

```bash
git add src/TNDrop.App/Core/IndicatorPalette.cs src/TNDrop.Tests/IndicatorPaletteTests.cs
git commit -m "feat: add IndicatorPalette single-resolution color derivation (v1.5)"
```

---

### Task B: 設定スキーマ (色/透明度/移行フラグ/新デフォルト) + サニタイズ + 移行関数

**Files:**
- Modify: `src/TNDrop.App/Core/AppSettings.cs`
- Modify: `src/TNDrop.App/Core/SettingsStore.cs` (Load のクランプ部)
- Create: `src/TNDrop.App/Core/SettingsMigration.cs`
- Test: `src/TNDrop.Tests/SettingsStoreTests.cs` (追記)、`src/TNDrop.Tests/SettingsMigrationTests.cs` (新規)

**Interfaces:**
- Consumes: `IndicatorPalette.TryParseHex` / `IndicatorPalette.DefaultColorHex` (Task A)
- Produces:
  - `AppSettings.IndicatorColor` (`string`, default `"#5AC8FA"`)
  - `AppSettings.IndicatorOpacityPercent` (`int`, default `100`)
  - `AppSettings.MinIndicatorOpacityPercent = 30` / `MaxIndicatorOpacityPercent = 100` (`const int`)
  - `AppSettings.AutoStartDefaultMigrated` (`bool`, default `false`)
  - `AppSettings.AutoStartEnabled` デフォルトが `true` に変更
  - `IndicatorStyle.Bulge` (enum 5 番目)、`AppSettings.IndicatorStyle` デフォルトが `Bulge` に変更
  - `SettingsMigration.ApplyAutoStartDefault(AppSettings s)` → `bool` (変更したら true)

- [ ] **Step 1: 失敗するテストを書く**

`src/TNDrop.Tests/SettingsStoreTests.cs` の末尾 (クラス閉じ括弧の前) に追記:

```csharp
    // v1.5: インジケーター色/透明度の新設定と、自動開始・スタイルの新デフォルト。
    [Fact]
    public void Load_returns_defaults_for_the_v1_5_settings_when_file_missing()
    {
        var s = new SettingsStore(_dir).Load();
        Assert.Equal("#5AC8FA", s.IndicatorColor);
        Assert.Equal(100, s.IndicatorOpacityPercent);
        Assert.True(s.AutoStartEnabled);            // v1.4 までは false だった
        Assert.False(s.AutoStartDefaultMigrated);   // 移行は App.OnStartup 側の責務
        Assert.Equal(IndicatorStyle.Bulge, s.IndicatorStyle);
    }

    [Fact]
    public void Load_clamps_indicator_opacity_into_range()
    {
        var store = new SettingsStore(_dir);
        store.Save(new AppSettings { IndicatorOpacityPercent = 29 });
        Assert.Equal(30, store.Load().IndicatorOpacityPercent);
        store.Save(new AppSettings { IndicatorOpacityPercent = 101 });
        Assert.Equal(100, store.Load().IndicatorOpacityPercent);
    }

    [Theory]
    [InlineData("xyz")]
    [InlineData("#12")]
    [InlineData("")]
    public void Load_falls_back_to_default_color_for_unparsable_hex(string bad)
    {
        var store = new SettingsStore(_dir);
        store.Save(new AppSettings { IndicatorColor = bad });
        Assert.Equal(IndicatorPalette.DefaultColorHex, store.Load().IndicatorColor);
    }

    [Fact]
    public void Load_keeps_a_valid_non_preset_color()
    {
        var store = new SettingsStore(_dir);
        store.Save(new AppSettings { IndicatorColor = "#123456" });
        Assert.Equal("#123456", store.Load().IndicatorColor);
    }

    // 旧バージョンの settings.json (キーなし) はコード側デフォルトで埋まる。
    // ここで AutoStartEnabled が true で埋まっても、実際の有効化は移行関数 +
    // 起動時自己修復が担う (SettingsMigrationTests を参照)。
    [Fact]
    public void Load_fills_in_v1_5_defaults_for_an_older_settings_file()
    {
        Directory.CreateDirectory(_dir);
        File.WriteAllText(Path.Combine(_dir, "settings.json"), """
            {
              "Edge": "Right",
              "IndicatorStyle": "Bar"
            }
            """);

        var s = new SettingsStore(_dir).Load();
        Assert.Equal(IndicatorStyle.Bar, s.IndicatorStyle); // 既存ユーザーのスタイルは尊重
        Assert.Equal("#5AC8FA", s.IndicatorColor);
        Assert.Equal(100, s.IndicatorOpacityPercent);
        Assert.False(s.AutoStartDefaultMigrated);
    }
```

`src/TNDrop.Tests/SettingsMigrationTests.cs` を新規作成:

```csharp
using TNDrop.Core;

// v1.5 設計書パート3: 自動開始のデフォルト有効化は「一回限りの移行」。
// フラグが立っていなければ一度だけ ON にしてフラグを立てる。フラグが立って
// いれば以降のユーザー操作 (OFF に戻した等) をそのまま尊重する。
public class SettingsMigrationTests
{
    [Fact]
    public void First_run_enables_autostart_and_sets_the_flag()
    {
        var s = new AppSettings { AutoStartEnabled = false, AutoStartDefaultMigrated = false };
        Assert.True(SettingsMigration.ApplyAutoStartDefault(s));
        Assert.True(s.AutoStartEnabled);
        Assert.True(s.AutoStartDefaultMigrated);
    }

    [Fact]
    public void Migrated_profile_keeps_the_users_choice()
    {
        var s = new AppSettings { AutoStartEnabled = false, AutoStartDefaultMigrated = true };
        Assert.False(SettingsMigration.ApplyAutoStartDefault(s));
        Assert.False(s.AutoStartEnabled);
    }

    [Fact]
    public void Fresh_install_defaults_still_get_the_flag_stamped()
    {
        var s = new AppSettings(); // AutoStartEnabled=true, flag=false
        Assert.True(SettingsMigration.ApplyAutoStartDefault(s));
        Assert.True(s.AutoStartEnabled);
        Assert.True(s.AutoStartDefaultMigrated);
    }
}
```

- [ ] **Step 2: テストが失敗することを確認**

Run: `dotnet test TNDrop.sln --filter "SettingsStoreTests|SettingsMigrationTests"`
Expected: コンパイルエラー (新プロパティ / SettingsMigration / Bulge 未定義) で FAIL。

- [ ] **Step 3: 実装**

`src/TNDrop.App/Core/AppSettings.cs`:

1. enum を変更: `public enum IndicatorStyle { Beacon, Bar, Pulse, Corner, Bulge }` (末尾に追加。JsonStringEnumConverter で文字列保存なので順序は互換に影響しない)。
2. `AutoStartEnabled` の行を `public bool AutoStartEnabled { get; set; } = true;` に変更し、行上に doc comment を追加:

```csharp
    /// <summary>v1.5 からデフォルト有効。既存プロファイルへの一回限りの適用は
    /// <see cref="SettingsMigration.ApplyAutoStartDefault"/> が担う。</summary>
    public bool AutoStartEnabled { get; set; } = true;
```

3. `IndicatorStyle` の行を `public IndicatorStyle IndicatorStyle { get; set; } = IndicatorStyle.Bulge;` に変更 (新規インストールのみ影響。既存ユーザーは保存値が読まれる)。
4. クラス末尾 (PasteOnClick の後) に追加:

```csharp
    /// <summary>Allowed range for <see cref="IndicatorOpacityPercent"/>; SettingsStore.Load
    /// clamps into this range. 下限 30 は「設定したのに全く見えない」事故防止 (v1.5)。</summary>
    public const int MinIndicatorOpacityPercent = 30;
    public const int MaxIndicatorOpacityPercent = 100;

    /// <summary>インジケーター基準色 "#RRGGBB" (v1.5)。ここから塗り/縁/リムを導出する
    /// のは IndicatorPalette.Resolve ただ 1 か所。パース不能な値は SettingsStore.Load が
    /// デフォルトに置き換えるので、読む側は常にパース可能とみなしてよい。</summary>
    public string IndicatorColor { get; set; } = IndicatorPalette.DefaultColorHex;

    /// <summary>フラッシュのピーク不透明度 (%) (v1.5)。IndicatorWindow が 1.0 の代わりに
    /// この値 /100 をピークに使う。</summary>
    public int IndicatorOpacityPercent { get; set; } = 100;

    /// <summary>自動開始デフォルト有効化 (v1.5) の一回限り移行が済んだか。
    /// <see cref="SettingsMigration.ApplyAutoStartDefault"/> だけが立てる。</summary>
    public bool AutoStartDefaultMigrated { get; set; } = false;
```

`src/TNDrop.App/Core/SettingsStore.cs` の Load 内、HistoryCapacity クランプの直後に追加:

```csharp
            // v1.5: 同じ「どこから来た値でも読む前に直す」方針。透明度は範囲クランプ、
            // 色はパース不能ならデフォルトへ (以降の読み手は常にパース可能とみなせる)。
            settings.IndicatorOpacityPercent = Math.Clamp(
                settings.IndicatorOpacityPercent,
                AppSettings.MinIndicatorOpacityPercent, AppSettings.MaxIndicatorOpacityPercent);
            if (!IndicatorPalette.TryParseHex(settings.IndicatorColor, out _))
            {
                settings.IndicatorColor = IndicatorPalette.DefaultColorHex;
            }
```

`src/TNDrop.App/Core/SettingsMigration.cs` を新規作成:

```csharp
namespace TNDrop.Core;

/// <summary>
/// 設定の一回限り移行 (v1.5)。App.OnStartup が設定ロード直後・レジストリ自己修復より
/// 前に呼び、true が返ったら保存する。純粋関数なので WPF なしでテストできる
/// (SettingsMigrationTests)。
/// </summary>
public static class SettingsMigration
{
    /// <summary>
    /// 自動開始デフォルト有効化 (設計書パート3)。未移行プロファイルを一度だけ ON に
    /// してフラグを立てる。移行済みなら何もしない (以降のユーザー操作を尊重)。
    /// v1.4 以前で意図的に OFF にしていたユーザーも一度だけ ON に戻る -- 「現状 OFF は
    /// ほぼ全員デフォルトのまま」という推定に基づく合意済みトレードオフ。
    /// </summary>
    public static bool ApplyAutoStartDefault(AppSettings s)
    {
        if (s.AutoStartDefaultMigrated)
        {
            return false;
        }

        s.AutoStartEnabled = true;
        s.AutoStartDefaultMigrated = true;
        return true;
    }
}
```

- [ ] **Step 4: テストが通ることを確認**

Run: `dotnet test TNDrop.sln`
Expected: 追加分を含め全 PASS。既存テストに `AutoStartEnabled` / `IndicatorStyle` の旧デフォルトを前提にしたものがあれば、この新デフォルトが正なので **テスト側を** 新デフォルトに合わせて更新する (実装を旧デフォルトへ戻さないこと)。

- [ ] **Step 5: コミット**

```bash
git add src/TNDrop.App/Core/AppSettings.cs src/TNDrop.App/Core/SettingsStore.cs src/TNDrop.App/Core/SettingsMigration.cs src/TNDrop.Tests/SettingsStoreTests.cs src/TNDrop.Tests/SettingsMigrationTests.cs
git commit -m "feat: indicator color/opacity settings, autostart default ON with one-time migration (v1.5)"
```

---

### Task C: 既存 4 スタイルの縁取り化と IndicatorPalette 適用 (IndicatorBrightness 廃止)

**Files:**
- Modify: `src/TNDrop.App/UI/IndicatorWindow.xaml` (視覚要素の全面書き換え)
- Modify: `src/TNDrop.App/UI/IndicatorWindow.xaml.cs`
- Delete: `src/TNDrop.Tests/IndicatorBrightnessTests.cs`
- Test: 既存スイート (この taskは WPF 視覚が主体。純粋ロジックは Task A で担保済み)

**Interfaces:**
- Consumes: `IndicatorPalette.Resolve` / `TryParseHex` / `DefaultColorHex` (Task A)、`AppSettings.IndicatorColor` / `IndicatorOpacityPercent` (Task B)
- Produces:
  - `IndicatorWindow.ApplyPalette()` — `public void`。Settings の色/透明度を読み直して全ブラシとピーク不透明度を更新する。コンストラクタからも呼ぶ。Task E の App セッターが呼ぶ。
  - XAML 要素名: `BeaconGroup` / `BarGroup` / `PulseGroup` / `CornerGroup` (各スタイルのコンテナ Grid)。Task D が同じ流儀で `BulgeGroup` を追加する。

- [ ] **Step 1: XAML を縁取り構成に書き換える**

`IndicatorWindow.xaml` の `<Grid IsHitTestVisible="False">` の中身と直前のコメントブロックを以下で置き換える (v1.3 Task E の長いコメントは廃止された仕組みの説明なので削除):

```xml
    <!-- Every group starts Opacity="0"; Flash() picks one, restarts its animation, then resets
         Opacity back to 0 when the animation completes (see IndicatorWindow.xaml.cs). The window
         is click-through (WindowStyles.MakeClickThrough), so IsHitTestVisible="False" documents
         intent rather than changing behavior.

         v1.5: each style is a "body + outline" pair inside one container Grid, and the flash
         animates the CONTAINER's opacity so both always light up together. The dark outline
         carries visibility on bright backgrounds, the bright fill on dark ones - the production
         feedback that started v1.5 was exactly "invisible on light desktops". All colors here are
         placeholders overwritten by ApplyPalette() in code-behind, the single funnel from the
         IndicatorColor setting through IndicatorPalette.Resolve; they are kept roughly correct as
         a design-time fallback only. Elements are x:Name'd individually because StaticResource is
         resolved once at parse time and could not be retargeted per-setting-change. -->
    <Grid IsHitTestVisible="False">
        <!-- Beacon: a soft 6px light with a 10px dark halo behind it, centered on the edge. -->
        <Grid x:Name="BeaconGroup" Opacity="0" VerticalAlignment="Center">
            <Rectangle x:Name="BeaconOutlineRect" Width="10" Height="140">
                <Rectangle.Fill>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                        <GradientStop x:Name="BeaconOutlineStart" Offset="0.0" Color="#00325F73" />
                        <GradientStop x:Name="BeaconOutlineMid" Offset="0.5" Color="#FF325F73" />
                        <GradientStop x:Name="BeaconOutlineEnd" Offset="1.0" Color="#00325F73" />
                    </LinearGradientBrush>
                </Rectangle.Fill>
            </Rectangle>
            <Rectangle x:Name="BeaconLight" Width="6" Height="140">
                <Rectangle.Fill>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                        <GradientStop x:Name="BeaconGradientStart" Offset="0.0" Color="#005AC8FA" />
                        <GradientStop x:Name="BeaconGradientMid" Offset="0.5" Color="#FF5AC8FA" />
                        <GradientStop x:Name="BeaconGradientEnd" Offset="1.0" Color="#005AC8FA" />
                    </LinearGradientBrush>
                </Rectangle.Fill>
            </Rectangle>
        </Grid>

        <!-- Bar: a 3px bar on a 7px dark backing, spanning the full work-area height. -->
        <Grid x:Name="BarGroup" Opacity="0" VerticalAlignment="Stretch">
            <Rectangle x:Name="BarOutlineRect" Width="7" VerticalAlignment="Stretch">
                <Rectangle.Fill>
                    <SolidColorBrush x:Name="BarOutlineBrush" Color="#FF325F73" />
                </Rectangle.Fill>
            </Rectangle>
            <Rectangle x:Name="BarLight" Width="3" VerticalAlignment="Stretch">
                <Rectangle.Fill>
                    <SolidColorBrush x:Name="BarBrush" Color="#FF5AC8FA" />
                </Rectangle.Fill>
            </Rectangle>
        </Grid>

        <!-- Pulse: an expanding circle; the 2px stroke keeps a dark rim while it grows. -->
        <Grid x:Name="PulseGroup" Opacity="0" VerticalAlignment="Center">
            <Ellipse x:Name="PulseCircle"
                     Width="8" Height="8"
                     StrokeThickness="2"
                     RenderTransformOrigin="0.5,0.5">
                <Ellipse.Fill>
                    <SolidColorBrush x:Name="PulseBrush" Color="#FF5AC8FA" />
                </Ellipse.Fill>
                <Ellipse.Stroke>
                    <SolidColorBrush x:Name="PulseStrokeBrush" Color="#FF325F73" />
                </Ellipse.Stroke>
                <Ellipse.RenderTransform>
                    <ScaleTransform x:Name="PulseScale" ScaleX="1" ScaleY="1" />
                </Ellipse.RenderTransform>
            </Ellipse>
        </Grid>

        <!-- Corner: a small outlined dot near the bottom of the work area. -->
        <Grid x:Name="CornerGroup" Opacity="0" VerticalAlignment="Bottom" Margin="0,0,0,24">
            <Ellipse x:Name="CornerDot"
                     Width="10" Height="10"
                     StrokeThickness="1.5">
                <Ellipse.Fill>
                    <SolidColorBrush x:Name="CornerBrush" Color="#FF5AC8FA" />
                </Ellipse.Fill>
                <Ellipse.Stroke>
                    <SolidColorBrush x:Name="CornerStrokeBrush" Color="#FF325F73" />
                </Ellipse.Stroke>
            </Ellipse>
        </Grid>
    </Grid>
```

注意: 旧要素 `CornerBrush` の個別色 (#FFA6E1FC) と `BeaconGradientStart/Mid/End` の旧値は廃止。コメントに "--" を含めないこと。

- [ ] **Step 2: code-behind を IndicatorPalette + コンテナ化に合わせて書き換える**

`IndicatorWindow.xaml.cs` の変更:

1. コンストラクタの `ApplyBrightenedColors();` を `ApplyPalette();` に置き換え。
2. `ApplyBrightenedColors` メソッドを削除し、代わりに以下を追加:

```csharp
    // v1.5: フラッシュのピーク不透明度 (設定 IndicatorOpacityPercent / 100)。
    // FlashElement / FlashPulse が 1.0 の代わりに使う。ApplyPalette が更新する。
    private double _peakOpacity = 1.0;

    /// <summary>
    /// 設定 (IndicatorColor / IndicatorOpacityPercent) を読み直し、全スタイルのブラシと
    /// ピーク不透明度を更新する (v1.5)。色は IndicatorPalette.Resolve の 1 回の解決から
    /// 全ブラシへ配る -- XAML のプレースホルダー色はデザイン時フォールバックにすぎない。
    /// コンストラクタのほか、設定変更時に App.SetIndicatorColor / SetIndicatorOpacityPercent
    /// から呼ばれる (即時反映)。Settings が null の設計時/テスト時はデフォルト色で動く。
    /// </summary>
    public void ApplyPalette()
    {
        var settings = global::TNDrop.App.Settings;
        if (!IndicatorPalette.TryParseHex(settings?.IndicatorColor, out var baseColor))
        {
            IndicatorPalette.TryParseHex(IndicatorPalette.DefaultColorHex, out baseColor);
        }

        var (fill, outline, _) = IndicatorPalette.Resolve(baseColor.R, baseColor.G, baseColor.B);
        var fillColor = System.Windows.Media.Color.FromArgb(255, fill.R, fill.G, fill.B);
        var fillTransparent = System.Windows.Media.Color.FromArgb(0, fill.R, fill.G, fill.B);
        var outlineColor = System.Windows.Media.Color.FromArgb(255, outline.R, outline.G, outline.B);
        var outlineTransparent = System.Windows.Media.Color.FromArgb(0, outline.R, outline.G, outline.B);

        BeaconGradientStart.Color = fillTransparent;
        BeaconGradientMid.Color = fillColor;
        BeaconGradientEnd.Color = fillTransparent;
        BeaconOutlineStart.Color = outlineTransparent;
        BeaconOutlineMid.Color = outlineColor;
        BeaconOutlineEnd.Color = outlineTransparent;
        BarBrush.Color = fillColor;
        BarOutlineBrush.Color = outlineColor;
        PulseBrush.Color = fillColor;
        PulseStrokeBrush.Color = outlineColor;
        CornerBrush.Color = fillColor;
        CornerStrokeBrush.Color = outlineColor;

        _peakOpacity = (settings?.IndicatorOpacityPercent ?? 100) / 100.0;
    }
```

3. `Flash` の switch をコンテナ対象に変更 (Bulge は Task D で追加するのでここではまだ 4 case のまま):

```csharp
                case IndicatorStyle.Beacon:
                    FlashElement(BeaconGroup, edge, BeaconDuration);
                    break;
                case IndicatorStyle.Bar:
                    FlashElement(BarGroup, edge, BarDuration);
                    break;
                case IndicatorStyle.Pulse:
                    FlashPulse(edge);
                    break;
                case IndicatorStyle.Corner:
                    FlashElement(CornerGroup, edge, CornerDuration);
                    break;
```

4. `FlashElement` のピークを `_peakOpacity` に変更 (`element.Opacity = 1.0;` → `element.Opacity = _peakOpacity;`、`new DoubleAnimation(1.0, 0.0, duration)` → `new DoubleAnimation(_peakOpacity, 0.0, duration)`)。
5. `FlashPulse` も同様: `PulseCircle` への Opacity 操作をすべて `PulseGroup` に付け替え (`PulseGroup.BeginAnimation(OpacityProperty, null)` / `PulseGroup.Opacity = _peakOpacity` / fade も `PulseGroup` に Begin、`fade.Completed` で `PulseGroup.Opacity = 0.0`)。ScaleTransform (`PulseScale`) は従来どおり `PulseCircle` 側。fade の From も `_peakOpacity`。
6. `FlashPulse` 内の `PulseCircle.HorizontalAlignment` は `PulseGroup.HorizontalAlignment` に変更。
7. ファイル末尾の `IndicatorBrightness` クラス全体 (doc comment 含む) を削除する。`IndicatorTiming` は残す。
8. クラス doc comment や残るコメント中の IndicatorBrightness への言及を IndicatorPalette に更新。

- [ ] **Step 3: 旧テストを削除してビルド確認**

`src/TNDrop.Tests/IndicatorBrightnessTests.cs` を削除。

Run: `dotnet test TNDrop.sln`
Expected: 全 PASS (IndicatorBrightness への参照が残っているとビルドエラーになるので、その場合は参照箇所をすべて更新)。

- [ ] **Step 4: 目視確認 (開発機)**

`dotnet run --project src/TNDrop.App` でアプリを起動し、設定画面 → 外観 → スタイルを 4 つ順に切り替え、白背景のウインドウを最大化した手前でフラッシュに暗い縁が付いて視認できることを確認する。確認後アプリを終了。

- [ ] **Step 5: コミット**

```bash
git add src/TNDrop.App/UI/IndicatorWindow.xaml src/TNDrop.App/UI/IndicatorWindow.xaml.cs
git rm src/TNDrop.Tests/IndicatorBrightnessTests.cs
git commit -m "feat: outline all 4 indicator styles via IndicatorPalette, retire IndicatorBrightness (v1.5)"
```

---

### Task D: 「ふくらみ」(Bulge) スタイルの実装

**Files:**
- Modify: `src/TNDrop.App/UI/IndicatorWindow.xaml` (BulgeGroup 追加)
- Modify: `src/TNDrop.App/UI/IndicatorWindow.xaml.cs` (FlashBulge + switch の Bulge case + ApplyPalette への Bulge ブラシ追加)

**Interfaces:**
- Consumes: `IndicatorStyle.Bulge` (Task B)、`IndicatorPalette.Resolve` の Rim (Task A)、`_peakOpacity` / `ApplyPalette` / コンテナ流儀 (Task C)
- Produces: `Flash(IndicatorStyle.Bulge, edge)` が動作する (呼び出し側の変更は不要 -- 既存の Flash 経路がそのまま使う)

- [ ] **Step 1: XAML に BulgeGroup を追加**

`IndicatorWindow.xaml` の `CornerGroup` の閉じタグの後、外側 Grid の閉じタグの前に追加:

```xml
        <!-- Bulge (v1.5, new default): an opaque dark blob that squishes out of the edge with a
             white glyph inside - figure/ground contrast it carries with it, so it reads on any
             background. Drawn for the LEFT edge; FlashBulge mirrors it in place for the right
             edge via BulgeMirror (ScaleX -1 about the horizontal center) and grows it from the
             edge via BulgeScale (animated 0 to 1, CenterX pinned to the edge-side face). The
             1.5px bright rim is the outline principle inverted, for dark backgrounds. -->
        <Grid x:Name="BulgeGroup" Opacity="0" Width="34" Height="150" VerticalAlignment="Center"
              HorizontalAlignment="Left">
            <Grid.RenderTransform>
                <TransformGroup>
                    <ScaleTransform x:Name="BulgeMirror" ScaleX="1" CenterX="17" />
                    <ScaleTransform x:Name="BulgeScale" ScaleX="0" CenterX="0" />
                </TransformGroup>
            </Grid.RenderTransform>
            <Path x:Name="BulgeBody"
                  Data="M 0,0 C 0,45 28,50 28,75 C 28,100 0,105 0,150 Z"
                  StrokeThickness="1.5">
                <Path.Fill>
                    <SolidColorBrush x:Name="BulgeBodyBrush" Color="#FF325F73" />
                </Path.Fill>
                <Path.Stroke>
                    <SolidColorBrush x:Name="BulgeRimBrush" Color="#FFBDE9FD" />
                </Path.Stroke>
            </Path>
            <!-- White copy glyph: two rounded rectangles, the front one filled with the body
                 color so it occludes the back one. Positioned in the blob's visual center. -->
            <Grid Width="16" Height="18" HorizontalAlignment="Left" VerticalAlignment="Center"
                  Margin="7,0,0,0">
                <Border Width="11" Height="13" CornerRadius="2.5"
                        BorderBrush="White" BorderThickness="1.5"
                        HorizontalAlignment="Right" VerticalAlignment="Top" />
                <Border x:Name="BulgeGlyphFront"
                        Width="11" Height="13" CornerRadius="2.5"
                        BorderBrush="White" BorderThickness="1.5"
                        HorizontalAlignment="Left" VerticalAlignment="Bottom">
                    <Border.Background>
                        <SolidColorBrush x:Name="BulgeGlyphFillBrush" Color="#FF325F73" />
                    </Border.Background>
                </Border>
            </Grid>
        </Grid>
```

- [ ] **Step 2: code-behind に FlashBulge を追加**

`IndicatorWindow.xaml.cs` の変更:

1. Duration 定数群の下に追加 (IndicatorTiming.Scale は既存 4 スタイルの一括ブーストであり、Bulge は設計書の絶対値をそのまま使う):

```csharp
    // v1.5 Bulge: 設計書パート4の絶対値。出現 350ms (軽いオーバーシュート) + 保持 400ms +
    // 退場 300ms (同じ経路を戻る, バウンスなし) = 約 1.05s。既存 4 スタイルの
    // IndicatorTiming.DurationBoost は「v1.3 で既存スタイルを一括で長くした」補正なので
    // Bulge には適用しない。
    private static readonly TimeSpan BulgeAppear = TimeSpan.FromMilliseconds(350);
    private static readonly TimeSpan BulgeHold = TimeSpan.FromMilliseconds(400);
    private static readonly TimeSpan BulgeRetract = TimeSpan.FromMilliseconds(300);
```

2. `Flash` の switch に case を追加:

```csharp
                case IndicatorStyle.Bulge:
                    FlashBulge(edge);
                    break;
```

3. `ApplyPalette` を更新: `var (fill, outline, _)` を `var (fill, outline, rim)` にし、末尾 (`_peakOpacity =` の前) に追加:

```csharp
        var rimColor = System.Windows.Media.Color.FromArgb(255, rim.R, rim.G, rim.B);
        BulgeBodyBrush.Color = outlineColor;
        BulgeGlyphFillBrush.Color = outlineColor;
        BulgeRimBrush.Color = rimColor;
```

4. `FlashPulse` の後に追加:

```csharp
    /// <summary>
    /// Bulge (v1.5): 画面端から不透明のこぶが「うにょっ」と出て、保持し、出てきた方向へ
    /// 縮んで戻る。1 本のキーフレームアニメーション (ScaleX) にまとめてあるので、連続
    /// キャプチャ時の restart 契約は他スタイルと同じ (途中でも先頭からやり直し)。出現の
    /// オーバーシュート (BackEase) はこぶの物性の性格づけとして意図的 -- 退場は spatial
    /// consistency (出た経路を戻る) を守りバウンスなしの EaseIn。右エッジは形状を
    /// BulgeMirror でその場鏡映し、成長の支点 (BulgeScale.CenterX) をエッジ側の面に置く。
    /// </summary>
    private void FlashBulge(EdgeSide edge)
    {
        BulgeGroup.HorizontalAlignment = edge == EdgeSide.Left
            ? System.Windows.HorizontalAlignment.Left
            : System.Windows.HorizontalAlignment.Right;
        BulgeMirror.ScaleX = edge == EdgeSide.Left ? 1.0 : -1.0;
        BulgeScale.CenterX = edge == EdgeSide.Left ? 0.0 : BulgeGroup.Width;

        BulgeGroup.BeginAnimation(OpacityProperty, null);
        BulgeScale.BeginAnimation(System.Windows.Media.ScaleTransform.ScaleXProperty, null);

        BulgeGroup.Opacity = _peakOpacity;
        BulgeScale.ScaleX = 0.0;

        var grow = new DoubleAnimationUsingKeyFrames
        {
            Duration = new Duration(BulgeAppear + BulgeHold + BulgeRetract),
            FillBehavior = FillBehavior.Stop,
        };
        grow.KeyFrames.Add(new EasingDoubleKeyFrame(1.0,
            KeyTime.FromTimeSpan(BulgeAppear),
            new BackEase { EasingMode = EasingMode.EaseOut, Amplitude = 0.3 }));
        grow.KeyFrames.Add(new DiscreteDoubleKeyFrame(1.0,
            KeyTime.FromTimeSpan(BulgeAppear + BulgeHold)));
        grow.KeyFrames.Add(new EasingDoubleKeyFrame(0.0,
            KeyTime.FromTimeSpan(BulgeAppear + BulgeHold + BulgeRetract),
            new QuadraticEase { EasingMode = EasingMode.EaseIn }));
        grow.Completed += (_, _) =>
        {
            BulgeScale.ScaleX = 0.0;
            BulgeGroup.Opacity = 0.0;
        };

        BulgeScale.BeginAnimation(System.Windows.Media.ScaleTransform.ScaleXProperty, grow);
    }
```

- [ ] **Step 3: ビルドと全テスト**

Run: `dotnet test TNDrop.sln`
Expected: 全 PASS。

- [ ] **Step 4: 目視確認 (開発機)**

`dotnet run --project src/TNDrop.App` で起動し、何かをコピーして Bulge が左端から出る/戻ることを確認 (新規プロファイルでない場合は設定でスタイルを切り替えてプレビュー。Task F 完了前は設定 UI に Bulge が無いため、%APPDATA%\TNDrop\settings.json の "IndicatorStyle" を "Bulge" に書き換えて再起動して確認する)。エッジ右設定でも鏡映で出ることを確認。確認後アプリを終了。

- [ ] **Step 5: コミット**

```bash
git add src/TNDrop.App/UI/IndicatorWindow.xaml src/TNDrop.App/UI/IndicatorWindow.xaml.cs
git commit -m "feat: add Bulge indicator style, the new default (v1.5)"
```

---

### Task E: App 配線 (移行の実行 + 色/透明度セッター)

**Files:**
- Modify: `src/TNDrop.App/App.xaml.cs`

**Interfaces:**
- Consumes: `SettingsMigration.ApplyAutoStartDefault` (Task B)、`IndicatorWindow.ApplyPalette` (Task C)
- Produces:
  - `App.SetIndicatorColor(string hex)` — `public static void`
  - `App.SetIndicatorOpacityPercent(int value)` — `public static void`
  - OnStartup での一回限り移行実行 (レジストリ自己修復より前)

- [ ] **Step 1: OnStartup に移行を挿入**

`App.xaml.cs` の OnStartup 内、自動開始の自己修復コメントブロック (「Self-heal: compares against the exact stored value...」、`var expectedAutoStartCommand = ...` の直前) の手前に挿入:

```csharp
            // v1.5: 自動開始デフォルト有効化の一回限り移行。自己修復より前に走らせることで、
            // 移行で ON になった分もこの同じ起動内でレジストリ Run 値まで書かれる。
            if (SettingsMigration.ApplyAutoStartDefault(Settings))
            {
                SaveSettings();
            }
```

- [ ] **Step 2: セッターを追加**

`SetIndicatorEnabled` メソッドの直後に追加:

```csharp
    /// <summary>インジケーター基準色 (v1.5)。塗り/縁/リムへの展開は IndicatorWindow.ApplyPalette
    /// 経由で IndicatorPalette.Resolve ただ 1 か所が行う。プレビューフラッシュは呼び出し元
    /// (SettingsWindow) が FlashIndicator で行う -- スタイル変更 (OnIndicatorStyleChanged) と
    /// 同じ分担。</summary>
    public static void SetIndicatorColor(string hex)
    {
        Settings.IndicatorColor = hex;
        SaveSettings();
        Indicator?.ApplyPalette();
    }

    /// <summary>フラッシュのピーク不透明度 % (v1.5)。範囲は設定 UI 側も 30..100 に固定して
    /// いるが、公開静的メソッドなのでここでもクランプする (SettingsStore.Load と同じ範囲)。</summary>
    public static void SetIndicatorOpacityPercent(int value)
    {
        Settings.IndicatorOpacityPercent = Math.Clamp(
            value, AppSettings.MinIndicatorOpacityPercent, AppSettings.MaxIndicatorOpacityPercent);
        SaveSettings();
        Indicator?.ApplyPalette();
    }
```

注意: `Indicator` は App の既存静的プロパティ (FlashIndicator が `Indicator?.Flash` している同じもの) を使う。

- [ ] **Step 3: ビルドと全テスト**

Run: `dotnet test TNDrop.sln`
Expected: 全 PASS。

- [ ] **Step 4: コミット**

```bash
git add src/TNDrop.App/App.xaml.cs
git commit -m "feat: wire autostart migration and indicator color/opacity setters (v1.5)"
```

---

### Task F: 設定 UI (Bulge ラジオ + 色スワッチ + 透明度スライダー + 文言)

**Files:**
- Modify: `src/TNDrop.App/UI/SettingsWindow.xaml`
- Modify: `src/TNDrop.App/UI/SettingsWindow.xaml.cs`
- Modify: `src/TNDrop.App/Resources/Strings.cs`
- Modify: `src/TNDrop.App/Resources/Strings.resx` (ja)
- Modify: `src/TNDrop.App/Resources/Strings.en.resx` (en)

**Interfaces:**
- Consumes: `App.SetIndicatorColor` / `App.SetIndicatorOpacityPercent` (Task E)、`App.SetIndicatorStyle` / `App.FlashIndicator` (既存)、`IndicatorStyle.Bulge` (Task B)、`AppSettings.Min/MaxIndicatorOpacityPercent` (Task B)
- Produces: 外観タブの新 UI。ほかのタスクからの依存はなし。

- [ ] **Step 1: 文言を 3 ファイルに追加**

`Strings.cs` の Settings セクション (SettingsIndicatorStyleCorner 付近) に追加:

```csharp
    public static string SettingsIndicatorStyleBulge => Get(nameof(SettingsIndicatorStyleBulge));
    public static string SettingsIndicatorColor => Get(nameof(SettingsIndicatorColor));
    public static string SettingsIndicatorColorDesc => Get(nameof(SettingsIndicatorColorDesc));
    public static string SettingsIndicatorOpacity => Get(nameof(SettingsIndicatorOpacity));
    public static string SettingsIndicatorOpacityValueFormat => Get(nameof(SettingsIndicatorOpacityValueFormat));
    public static string SettingsIndicatorOpacityDesc => Get(nameof(SettingsIndicatorOpacityDesc));
    public static string SettingsIndicatorColorSkyBlue => Get(nameof(SettingsIndicatorColorSkyBlue));
    public static string SettingsIndicatorColorBlue => Get(nameof(SettingsIndicatorColorBlue));
    public static string SettingsIndicatorColorGreen => Get(nameof(SettingsIndicatorColorGreen));
    public static string SettingsIndicatorColorOrange => Get(nameof(SettingsIndicatorColorOrange));
    public static string SettingsIndicatorColorRed => Get(nameof(SettingsIndicatorColorRed));
    public static string SettingsIndicatorColorPurple => Get(nameof(SettingsIndicatorColorPurple));
    public static string SettingsIndicatorColorWhite => Get(nameof(SettingsIndicatorColorWhite));
    public static string SettingsIndicatorColorGray => Get(nameof(SettingsIndicatorColorGray));
```

`Strings.resx` (ja) に `<data>` エントリを追加 (既存エントリと同じ書式で。value は以下):

| name | value (ja) |
| --- | --- |
| SettingsIndicatorStyleBulge | ふくらみ |
| SettingsIndicatorColor | 色 |
| SettingsIndicatorColorDesc | フラッシュの基準色。縁取りの濃色は自動で導出されます。 |
| SettingsIndicatorOpacity | 透明度 |
| SettingsIndicatorOpacityValueFormat | {0}% |
| SettingsIndicatorOpacityDesc | フラッシュの濃さ。100% が最も見えやすい設定です。 |
| SettingsIndicatorColorSkyBlue | 水色 |
| SettingsIndicatorColorBlue | 青 |
| SettingsIndicatorColorGreen | 緑 |
| SettingsIndicatorColorOrange | オレンジ |
| SettingsIndicatorColorRed | レッド |
| SettingsIndicatorColorPurple | パープル |
| SettingsIndicatorColorWhite | 白 |
| SettingsIndicatorColorGray | グレー |

`Strings.en.resx` (en) に同じ name で追加:

| name | value (en) |
| --- | --- |
| SettingsIndicatorStyleBulge | Bulge |
| SettingsIndicatorColor | Color |
| SettingsIndicatorColorDesc | Base color of the flash. The dark outline shade is derived automatically. |
| SettingsIndicatorOpacity | Opacity |
| SettingsIndicatorOpacityValueFormat | {0}% |
| SettingsIndicatorOpacityDesc | Strength of the flash. 100% is the most visible. |
| SettingsIndicatorColorSkyBlue | Sky Blue |
| SettingsIndicatorColorBlue | Blue |
| SettingsIndicatorColorGreen | Green |
| SettingsIndicatorColorOrange | Orange |
| SettingsIndicatorColorRed | Red |
| SettingsIndicatorColorPurple | Purple |
| SettingsIndicatorColorWhite | White |
| SettingsIndicatorColorGray | Gray |

- [ ] **Step 2: XAML に色スワッチスタイルと新フィールドグループを追加**

`SettingsWindow.xaml` の `Window.Resources` 内 (`Settings.NavButton` スタイルの後) に追加:

```xml
        <!-- Circular color swatch (v1.5): a RadioButton whose Background carries the preset
             color (set per instance in code-behind, where the swatches are built from the same
             preset table that maps hex to name). Re-templated for the same reason as
             CheckBox/RadioButton above - every color must be a Setter here, not an ambient
             default. The outer ring shows the checked state. -->
        <Style x:Key="Settings.ColorSwatch" TargetType="RadioButton">
            <Setter Property="Margin" Value="0,0,10,0" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="FocusVisualStyle" Value="{StaticResource Settings.FocusVisual}" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Grid Width="26" Height="26">
                            <Ellipse x:Name="Ring" Stroke="Transparent" StrokeThickness="2" />
                            <Ellipse Width="18" Height="18"
                                     Fill="{TemplateBinding Background}"
                                     Stroke="#33FFFFFF" StrokeThickness="1" />
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="Ring" Property="Stroke" Value="{StaticResource Settings.Foreground}" />
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Ring" Property="Stroke" Value="{StaticResource Settings.HoverBorder}" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.5" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
```

外観タブ: `IndicatorStyleLabelText` のラジオ列の先頭に Bulge ラジオを追加 (新デフォルトなので先頭):

```xml
                                <RadioButton x:Name="IndicatorBulgeRadio" GroupName="IndicatorStyle" />
```

(既存の `IndicatorBeaconRadio` の行の直前に挿入。)

`IndicatorStyle` フィールドグループの閉じタグの後、`LanguageLabelText` グループの前に追加:

```xml
                        <StackPanel Style="{StaticResource Settings.FieldGroup}">
                            <TextBlock x:Name="IndicatorColorLabelText" Style="{StaticResource Settings.SectionLabel}" />
                            <StackPanel x:Name="IndicatorColorSwatchPanel" Orientation="Horizontal" />
                            <TextBlock x:Name="IndicatorColorDescText" Style="{StaticResource Settings.HintText}" TextWrapping="Wrap" />
                        </StackPanel>

                        <StackPanel Style="{StaticResource Settings.FieldGroup}">
                            <TextBlock x:Name="IndicatorOpacityLabelText" Style="{StaticResource Settings.SectionLabel}" />
                            <Slider x:Name="IndicatorOpacitySlider" Width="220" HorizontalAlignment="Left" />
                            <TextBlock x:Name="IndicatorOpacityValueText" Style="{StaticResource Settings.HintText}" />
                            <TextBlock x:Name="IndicatorOpacityDescText" Style="{StaticResource Settings.HintText}" TextWrapping="Wrap" />
                        </StackPanel>
```

- [ ] **Step 3: code-behind の配線**

`SettingsWindow.xaml.cs` の変更 (既存の外観タブ初期化コードのある場所に合わせて):

1. Bulge ラジオ (既存 4 つと同じ並びに追加):

```csharp
        IndicatorBulgeRadio.Content = Strings.SettingsIndicatorStyleBulge;
        IndicatorBulgeRadio.IsChecked = settings.IndicatorStyle == IndicatorStyle.Bulge;
        IndicatorBulgeRadio.Checked += (_, _) => OnIndicatorStyleChanged(IndicatorStyle.Bulge);
```

2. `UpdateIndicatorStyleEnabled` に追記 (Bulge ラジオ + 新グループも indicator OFF で dim する。TextBlock の Opacity 手動設定の理由は既存コメントのとおり):

```csharp
        IndicatorBulgeRadio.IsEnabled = enabled;
        IndicatorColorLabelText.IsEnabled = enabled;
        IndicatorColorLabelText.Opacity = enabled ? 1.0 : 0.4;
        IndicatorColorSwatchPanel.IsEnabled = enabled;
        IndicatorColorDescText.IsEnabled = enabled;
        IndicatorColorDescText.Opacity = enabled ? 1.0 : 0.4;
        IndicatorOpacityLabelText.IsEnabled = enabled;
        IndicatorOpacityLabelText.Opacity = enabled ? 1.0 : 0.4;
        IndicatorOpacitySlider.IsEnabled = enabled;
        IndicatorOpacityValueText.IsEnabled = enabled;
        IndicatorOpacityValueText.Opacity = enabled ? 1.0 : 0.4;
        IndicatorOpacityDescText.IsEnabled = enabled;
        IndicatorOpacityDescText.Opacity = enabled ? 1.0 : 0.4;
```

3. スタイルラジオ初期化ブロックの後に色スワッチ + スライダー初期化を追加:

```csharp
        // インジケーターの色 (v1.5): プリセット 8 色のスワッチ。保存値は HEX そのもの。
        // プリセット外の (手書きされた) 有効な HEX はどのスワッチも選択されないまま尊重
        // される -- SettingsStore.Load がパース可能性は保証済み。
        IndicatorColorLabelText.Text = Strings.SettingsIndicatorColor;
        IndicatorColorDescText.Text = Strings.SettingsIndicatorColorDesc;
        (string Hex, string Name)[] colorPresets =
        {
            ("#5AC8FA", Strings.SettingsIndicatorColorSkyBlue),
            ("#0A84FF", Strings.SettingsIndicatorColorBlue),
            ("#34C759", Strings.SettingsIndicatorColorGreen),
            ("#FF9F0A", Strings.SettingsIndicatorColorOrange),
            ("#FF453A", Strings.SettingsIndicatorColorRed),
            ("#BF5AF2", Strings.SettingsIndicatorColorPurple),
            ("#FFFFFF", Strings.SettingsIndicatorColorWhite),
            ("#8E8E93", Strings.SettingsIndicatorColorGray),
        };
        foreach (var (hex, name) in colorPresets)
        {
            var swatch = new RadioButton
            {
                Style = (Style)FindResource("Settings.ColorSwatch"),
                GroupName = "IndicatorColor",
                Background = new SolidColorBrush(
                    (System.Windows.Media.Color)System.Windows.Media.ColorConverter.ConvertFromString(hex)),
                ToolTip = name,
                IsChecked = string.Equals(settings.IndicatorColor, hex, StringComparison.OrdinalIgnoreCase),
            };
            swatch.Checked += (_, _) => OnIndicatorColorChanged(hex);
            IndicatorColorSwatchPanel.Children.Add(swatch);
        }

        // 透明度 (v1.5): RetractDelaySlider と同じ Slider 部品・同じ wiring 形。
        IndicatorOpacityLabelText.Text = Strings.SettingsIndicatorOpacity;
        IndicatorOpacityDescText.Text = Strings.SettingsIndicatorOpacityDesc;
        IndicatorOpacitySlider.Minimum = AppSettings.MinIndicatorOpacityPercent;
        IndicatorOpacitySlider.Maximum = AppSettings.MaxIndicatorOpacityPercent;
        IndicatorOpacitySlider.TickFrequency = 5;
        IndicatorOpacitySlider.IsSnapToTickEnabled = true;
        IndicatorOpacitySlider.Value = settings.IndicatorOpacityPercent;
        UpdateIndicatorOpacityText(settings.IndicatorOpacityPercent);
        IndicatorOpacitySlider.ValueChanged += (_, e) =>
        {
            var value = (int)Math.Round(e.NewValue);
            UpdateIndicatorOpacityText(value);
            OnIndicatorOpacityChanged(value);
        };
```

4. ハンドラ 3 つを `OnIndicatorStyleChanged` の後に追加 (`_initializing` ガードは同メソッドと同じ流儀):

```csharp
    /// <summary>色変更 (v1.5): 適用してから、スタイル変更と同じテストフラッシュで
    /// 「その色が画面端でどう見えるか」を見せる。</summary>
    private void OnIndicatorColorChanged(string hex)
    {
        if (_initializing)
        {
            return;
        }

        global::TNDrop.App.SetIndicatorColor(hex);
        global::TNDrop.App.FlashIndicator(
            global::TNDrop.App.Settings.IndicatorStyle, global::TNDrop.App.Settings.Edge);
    }

    /// <summary>透明度変更 (v1.5): ドラッグ中も毎変更でフラッシュを restart するので、
    /// つまみを動かしている間は実質プレビューが出続ける。</summary>
    private void OnIndicatorOpacityChanged(int value)
    {
        if (_initializing)
        {
            return;
        }

        global::TNDrop.App.SetIndicatorOpacityPercent(value);
        global::TNDrop.App.FlashIndicator(
            global::TNDrop.App.Settings.IndicatorStyle, global::TNDrop.App.Settings.Edge);
    }

    private void UpdateIndicatorOpacityText(int value)
    {
        IndicatorOpacityValueText.Text = string.Format(
            System.Globalization.CultureInfo.CurrentCulture,
            Strings.SettingsIndicatorOpacityValueFormat, value);
    }
```

注意: `RetractDelaySlider` の ValueChanged が `_initializing` をどう扱っているかを実装前に確認し、同じ流儀に合わせる (Value 設定時に発火するイベントの初期化ガード)。`using` に不足があれば追加 (`System.Windows.Controls`, `System.Windows.Media` は既存 using を確認)。

- [ ] **Step 4: ビルドと全テスト**

Run: `dotnet test TNDrop.sln`
Expected: 全 PASS。

- [ ] **Step 5: 目視確認 (開発機)**

`dotnet run --project src/TNDrop.App` で起動し、設定 → 外観で:
1. スタイルに「ふくらみ」が先頭に出て、選択でプレビューが出る。
2. 色スワッチ 8 個が並び、クリックで即プレビュー + 再起動後も保持。
3. 透明度スライダーで薄くなり、30% 未満にできない。
4. 「インジケーターを表示」を OFF にするとスタイル/色/透明度がまとめて dim する。
5. 言語を English にして再起動し、新文言が英語で出る。
確認後アプリを終了。

- [ ] **Step 6: コミット**

```bash
git add src/TNDrop.App/UI/SettingsWindow.xaml src/TNDrop.App/UI/SettingsWindow.xaml.cs src/TNDrop.App/Resources/Strings.cs src/TNDrop.App/Resources/Strings.resx src/TNDrop.App/Resources/Strings.en.resx
git commit -m "feat: settings UI for indicator color/opacity and Bulge style (v1.5)"
```

---

### Task G: ドキュメント更新 (手動テストチェックリスト + CLAUDE.md ポインタ)

**Files:**
- Modify: `docs/manual-test-checklist.md`
- Modify: `CLAUDE.md` (§9 の設計書/実装計画ポインタ)

**Interfaces:**
- Consumes: なし (文書のみ)
- Produces: なし

- [ ] **Step 1: 手動テストチェックリストに v1.5 項目を追加**

`docs/manual-test-checklist.md` を開き、既存の項目番号形式 (例: 28-1 のような 節-連番) に合わせて v1.5 の節を追加する。内容は設計書「テスト」節のこの 4 項目 (文言は既存チェックリストの流儀に合わせて整える):

1. [必須] 白いウインドウ (メモ帳最大化など) を手前にした状態で、5 スタイルすべてのフラッシュが視認できること。
2. 色スワッチ・透明度スライダーの変更が即座にプレビューされ、アプリ再起動後も保持されること。
3. [必須] v1.4.x からの上書きインストール後の初回起動で「起動時に自動で開始する」が ON になり、サインアウト → サインインで TNDrop が自動起動すること。設定で OFF に戻した後の再起動で OFF が保持されること。
4. マルチモニター / DPI 混在環境で、ふくらみの出現・退場が設定したエッジの正しい位置に出ること (左右両エッジ)。

- [ ] **Step 2: CLAUDE.md の §9 ポインタを更新**

`CLAUDE.md` の「設計書:」の列挙に `docs/superpowers/specs/2026-08-24-tndrop-v1.5-indicator-visibility-design.md (v1.5)` を、「実装計画:」の列挙に `docs/superpowers/plans/2026-08-24-tndrop-v1.5-indicator-visibility.md (v1.5)` を追記する。

- [ ] **Step 3: コミット**

```bash
git add docs/manual-test-checklist.md CLAUDE.md
git commit -m "docs: v1.5 manual test items and doc pointers"
```

---

## 計画外 (このリリースの後工程として別途)

- バージョン番号の更新 (AppVersion / setup.iss) とインストーラービルド (`build.ps1`)、インストーラースキル (`inno-setup-packaging`) の読み込みは、リリース作業として本計画の外で行う。
- 手動テストチェックリストの実機消化 (特に [必須] 項目) はユーザーが行う。
