# TNDrop v1.7.1 静止カーソルでのシェルフ出入りループ修正 実装計画

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** カーソルがトリガー位置に静止し続けてもシェルフが出入りを繰り返さないよう、格納タイマー tick に実カーソル座標による物理チェックを追加する。

**Architecture:** 純関数 `ShelfRetract.CursorHolds` (点 in 2 矩形の判定) を追加し、`ShelfWindow.OnRetractTick` の抑止条件に配線する。トリガー矩形は `Place()` でキャッシュ (EdgeTriggerWindow と同じ流儀)。アーム側 (`ShouldArm` / `ArmRetractIfPointerOutside`) は一切変更しない。

**Tech Stack:** .NET 10 / WPF、既存 BCL のみ、xunit。

**正とする設計書:** `docs/superpowers/specs/2026-08-31-tndrop-v1.7.1-hover-hold-design.md`。

## Global Constraints

- ソースは UTF-8 (BOM なし) LF。新規 NuGet 禁止。新しいタイマーを追加しない。
- **アーム側は変更しない**: `ShelfRetract.ShouldArm` のシグネチャ・本文、`ArmRetractIfPointerOutside`、`IsPointerInside` は不変。物理チェックは tick の抑止条件のみ (設計書 §2「変えないもの」— 可視シェルフにタイマー無し = 詰み、の不変条件を守る)。
- カーソル取得は `MonitorGeometry.CursorDip(WorkArea)`、トリガー矩形は `ShelfPlacement.TriggerRect`、シェルフ矩形は配置済み `_rect` — いずれも既存の唯一の算出元を再利用し、判定規則を二重に持たない。
- Win32 失敗時は false (従来挙動に倒す。開いたまま詰む方向に倒さない)。
- 既存 `IsCursorOverShelf` (フライアウト用) の挙動を変えない。
- コミットメッセージ末尾: `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>`。各タスクで `dotnet test` 全 green。

---

### Task 1: CursorHolds 純関数 + OnRetractTick 配線

**Files:**
- Modify: `src/TNDrop.App/UI/ShelfRetract.cs` (`ShouldArm` の後に追加。ShouldArm 本文は触らない)
- Modify: `src/TNDrop.App/UI/ShelfWindow.xaml.cs` (`Place()` 内のキャッシュ追加、`OnRetractTick` の抑止条件、private ヘルパ)
- Test: `src/TNDrop.Tests/ShelfRetractTests.cs` (既存ファイルに追記)

**Interfaces:**
- Consumes: `ShelfPlacement.Rect(double X, double Y, double W, double H)` (record struct, ShelfPlacement.cs:19) / `ShelfPlacement.TriggerRect(Rect workArea, EdgeSide edge, int proximityPx, int hotZonePercent, TriggerAlign align)` (ShelfPlacement.cs:28) / `MonitorGeometry.CursorDip(WorkArea area)` → `(double X, double Y)` / ShelfWindow の既存フィールド `_area` (MonitorGeometry.WorkArea) `_rect` (配置済みシェルフ矩形) `_settings` (AppSettings?) `_placed` (bool)
- Produces: `public static bool ShelfRetract.CursorHolds(double cursorX, double cursorY, ShelfPlacement.Rect shelfRect, ShelfPlacement.Rect triggerRect)` (Task 2 は使わない — 末端)

- [ ] **Step 1: 失敗するテストを書く**

`src/TNDrop.Tests/ShelfRetractTests.cs` の既存クラスに追記 (既存テストは触らない。ファイル冒頭の using に `TNDrop.UI` が既にあること、Rect の名前空間参照が通ることを確認して合わせる):

```csharp
// --- v1.7.1 CursorHolds: 静止カーソルの物理チェック (設計書 §3) ---
// shelf = (0, 30, 340, 540)、trigger = (0, 200, 3, 240) : 左端・トリガーはシェルフに包含
// trigger2 = (0, 0, 3, 100) : シェルフ上端より上にはみ出すトリガー (Top align + 大ホットゾーン相当)

private static readonly ShelfPlacement.Rect Shelf = new(0, 30, 340, 540);
private static readonly ShelfPlacement.Rect TriggerInside = new(0, 200, 3, 240);
private static readonly ShelfPlacement.Rect TriggerSticksOut = new(0, 0, 3, 100);

[Theory]
[InlineData(170, 300)]  // シェルフ中央
[InlineData(1, 210)]    // トリガー帯内 (シェルフにも含まれる)
[InlineData(0, 30)]     // シェルフ左上角 (境界は内側扱い)
[InlineData(340, 570)]  // シェルフ右下角 (境界は内側扱い)
public void CursorHolds_InsideShelf_True(double x, double y)
{
    Assert.True(ShelfRetract.CursorHolds(x, y, Shelf, TriggerInside));
}

[Fact]
public void CursorHolds_InsideTriggerButOutsideShelf_True()
{
    // シェルフ上端 (Y=30) より上・トリガー帯 (Y=0..100) の中: トリガー矩形の項が拾う
    Assert.True(ShelfRetract.CursorHolds(1, 10, Shelf, TriggerSticksOut));
}

[Theory]
[InlineData(341, 300)]  // シェルフの右外
[InlineData(170, 29)]   // シェルフの上外 (トリガーも外)
[InlineData(170, 571)]  // シェルフの下外
[InlineData(4, 10)]     // はみ出しトリガーの右外・シェルフの上外
public void CursorHolds_OutsideBoth_False(double x, double y)
{
    Assert.False(ShelfRetract.CursorHolds(x, y, Shelf, TriggerSticksOut));
}
```

- [ ] **Step 2: 実行して失敗を確認**

Run: `dotnet test --filter ShelfRetractTests`
Expected: コンパイルエラー (CursorHolds 未定義) で FAIL

- [ ] **Step 3: 純関数を実装**

`src/TNDrop.App/UI/ShelfRetract.cs` の `ShouldArm` の後 (クラス内) に追加:

```csharp
/// <summary>
/// v1.7.1: "is the cursor PHYSICALLY over the shelf or the trigger band right now?" --
/// answered from coordinates, not mouse events. A stationary cursor generates no
/// WM_MOUSEMOVE, so a shelf that slides in underneath it never receives MouseEnter and
/// every event-derived term of IsPointerInside stays false; the retract tick then slides
/// the shelf out from under a cursor that is sitting on it, the trigger band comes back,
/// and the loop repeats (same failure class StackFlyout measured and fixed with
/// CursorOverShelf). This is the tick-side suppression term only -- arming is untouched,
/// so the timer keeps running as the poll that re-asks this every tick.
/// <para>The trigger rect is checked as well as the shelf rect because Top/Bottom align
/// with a large hot zone can push the band outside the shelf's vertical span; "the cursor
/// is parked on the trigger" must hold the shelf open in every configuration.</para>
/// <para>Boundaries count as inside: erring toward "holding" keeps the shelf open one
/// tick longer, which is the harmless direction.</para>
/// </summary>
public static bool CursorHolds(double cursorX, double cursorY,
    ShelfPlacement.Rect shelfRect, ShelfPlacement.Rect triggerRect)
{
    return Contains(shelfRect, cursorX, cursorY) || Contains(triggerRect, cursorX, cursorY);
}

private static bool Contains(ShelfPlacement.Rect r, double x, double y) =>
    x >= r.X && x <= r.X + r.W && y >= r.Y && y <= r.Y + r.H;
```

(ShelfRetract.cs に `ShelfPlacement` への参照が無い場合は同じ名前空間 `TNDrop.UI` なので修飾不要のはず — 実物を確認して合わせる。)

- [ ] **Step 4: テストが通ることを確認**

Run: `dotnet test --filter ShelfRetractTests`
Expected: PASS (既存 + 新規 9 ケース)。既存の ShouldArm テストが**無変更で** green であること。

- [ ] **Step 5: ShelfWindow に配線**

`src/TNDrop.App/UI/ShelfWindow.xaml.cs`:

(a) フィールド追加 (`_rect` の近く):

```csharp
// v1.7.1: トリガー帯の矩形キャッシュ。Place() でのみ更新 (EdgeTriggerWindow の
// _hintTriggerRect と同じ流儀 -- tick の hot path で TriggerRect を再計算しない)。
// 算出は EdgeTriggerWindow.Place と同じ唯一の関数 ShelfPlacement.TriggerRect。
private ShelfPlacement.Rect _holdTriggerRect;
```

(b) `Place()` 内、`_rect` を保存している箇所の直後に追加 (Place のシグネチャで settings `s` と `area` が手元にあることを確認):

```csharp
_holdTriggerRect = ShelfPlacement.TriggerRect(
    new ShelfPlacement.Rect(area.X, area.Y, area.W, area.H),
    s.Edge, s.TriggerProximityPx, s.HotZonePercent, s.TriggerAlign);
```

(c) private ヘルパ (`OnRetractTick` の近く):

```csharp
/// <summary>
/// v1.7.1: the retract tick's physical-cursor term. See ShelfRetract.CursorHolds for the
/// full rationale (stationary cursor = no mouse events = every event-derived term dead).
/// Win32/geometry failure returns false so behaviour degrades to the pre-v1.7.1 rules
/// rather than pinning the shelf open.
/// </summary>
private bool IsCursorHoldingShelf()
{
    if (!_placed)
    {
        return false;
    }

    try
    {
        var (cursorX, cursorY) = MonitorGeometry.CursorDip(_area);
        return ShelfRetract.CursorHolds(cursorX, cursorY, _rect, _holdTriggerRect);
    }
    catch
    {
        return false;
    }
}
```

(d) `OnRetractTick` の抑止分岐 (583-605 行付近、行番号はずれている可能性あり — 本文で探す) を変更:

```csharp
if (IsPointerInside)
{
    ...
}
```
↓
```csharp
if (IsPointerInside || IsCursorHoldingShelf())
{
    // Suppressed, not cancelled (既存コメントを保持)。IsCursorHoldingShelf は
    // 静止カーソル対策 (v1.7.1): イベントが死んでいても座標で「上にいる」を検知する。
    ...
}
```

既存の分岐本文 (`_retractTimer.Stop(); _retractTimer.Start(); return;`) と既存コメントは保持し、条件と 1 行の補足コメントだけ足す。**`ArmRetractIfPointerOutside` / `ShouldArm` / `IsPointerInside` / `IsCursorOverShelf` は一切変更しない。**

- [ ] **Step 6: ビルド・全テスト**

Run: `dotnet build` clean (新規警告なし)、`dotnet test` 全 green (507 + 新規 9 = 516 目安)。

- [ ] **Step 7: Commit**

```bash
git add src/TNDrop.App/UI/ShelfRetract.cs src/TNDrop.App/UI/ShelfWindow.xaml.cs src/TNDrop.Tests/ShelfRetractTests.cs
git commit -m "fix: hold the shelf open while the cursor physically sits on it or the trigger band (v1.7.1 Task 1)"
```

---

### Task 2: ドキュメント・バージョン・最終検証

**Files:**
- Modify: `docs/manual-test-checklist.md` (§32 の表末尾の後、§32 側の `---` の後に §33 を挿入 — §31→§32 と同じ「`---` が新セクション見出しの直前に来る」体裁)
- Modify: `assets/README.html` ("1.7.0" を検索して全置換 → 1.7.1、置換後 "1.7.0" が 0 件。機能説明の追記は不要 — 挙動修正のみ)
- Modify: `src/TNDrop.App/TNDrop.App.csproj` (`<Version>1.7.1</Version>`)
- Modify: `installer/setup.iss` (`#define MyAppVersion    "1.7.1"` のみ。AppId 不変)
- Modify: `CLAUDE.md` (§9 実装計画リスト末尾に `docs/superpowers/plans/2026-08-31-tndrop-v1.7.1-hover-hold.md (v1.7.1)` を追記)

**Interfaces:** なし (ドキュメントのみ)

- [ ] **Step 1: manual-test-checklist.md に §33 を追加**

```markdown

## 33. v1.7.1 修正 (静止カーソルでのシェルフ出入りループ)

| # | 操作 | 期待結果 | 結果 |
|---|------|----------|------|
| 33-1 | [必須] カーソルをトリガー位置に置いたまま動かさず 10 秒以上待つ | シェルフが出て、**出たまま** (出入りを繰り返さない) | |
| 33-2 | 33-1 の状態からカーソルをシェルフ外へどける | 格納遅延後に従来どおり格納する | |
| 33-3 | 通常のホバー操作 (出す → カードにマウスを乗せる → 離れる) | 従来どおり: 乗っている間は格納されず、離れると格納遅延後に格納 | |
| 33-4 | 格納遅延を最短 (100ms) にして 33-1 を再確認 | 最短設定でも出たままになる | |
```

- [ ] **Step 2: バージョンと CLAUDE.md**

csproj / setup.iss / README ×3 を 1.7.1 に (README は "1.7.0" 全置換後に 0 件確認)。CLAUDE.md §9 に本計画ポインタ追記。

- [ ] **Step 3: 最終検証**

Run: `dotnet test` → 全 green
Run: `powershell -File build.ps1` → `dist\TNDrop-Setup-1.7.1.exe` ができること

- [ ] **Step 4: Commit**

```bash
git add docs/manual-test-checklist.md assets/README.html src/TNDrop.App/TNDrop.App.csproj installer/setup.iss CLAUDE.md
git commit -m "docs: v1.7.1 checklist section 33, version bump"
```

---

## 実装順序と依存

Task 1 → Task 2 の直列。

## 実機テストゲート (リリース前・利用者実施)

§33 の [必須] 33-1 (静止カーソルで出たまま) は自動テストでは再現できない (WPF のイベント配送そのものが対象のため)。リリース判断の前に実機で消化すること。開発機は本物の TNDrop が Mutex を保持しているため、今回も GUI スモークは §33 に委ねる。
